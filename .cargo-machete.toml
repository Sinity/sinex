# False positives for cargo machete
# These dependencies are actually used but cargo machete can't detect them

[ignored]
# Used in build.rs files
sinex-ingestd = ["prost"]
sinex-node-sdk = ["tonic-build"]

# Dev dependencies (correctly in dev-dependencies section)
# These show up as unused but are in [dev-dependencies]
sinex-services = ["sinex-test-utils"]
sinex-terminal-command-canonicalizer = ["sinex-test-utils"]
sinex-health-aggregator = ["sinex-test-utils"] 

# Used in binaries that need logging setup
sinex-desktop-node = ["tracing-subscriber"]
sinex-fs-watcher = ["tracing-subscriber"]
sinex-system-node = ["tracing-subscriber"]
sinex-terminal-node = ["tracing-subscriber"]

# Platform-specific or conditional dependencies
sinex-terminal-node = ["xterm-query"]
sinex-system-node = ["battery", "procfs", "systemstat"]
