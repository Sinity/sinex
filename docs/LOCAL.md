# Sinex Codebase Micro-Refactoring Analysis Report

Generated by 10 parallel systematic-refactorer agents analyzing the entire codebase.

## Agent 1: sinex-core Analysis

Based on my comprehensive analysis of the Sinex event-driven data capture system, here's my deep micro-refactoring analysis for the **crate/lib/sinex-services/** section:

## ARCHITECTURAL CONTEXT:
- **Key patterns observed**: The project uses sophisticated SinexError with rich context builder methods (.with_context(), .with_path(), .with_duration()), SQLX with repository pattern via DbPoolExt trait extensions, ULID primary keys with time-ordered properties, comprehensive testing via #[sinex_test] macro, and heavy use of workspace dependencies with consistent patterns.
- **Available utilities not being used**: SinexError's extensive builder methods, SeaQuery helpers for query construction (seaquery_helpers::SeaQueryUlidExt), ULID helper functions (ulid_to_uuid), and structured query builders available in the repository pattern.
- **Inconsistencies with project standards**: The services layer reimplements error handling instead of leveraging SinexError, uses manual SQL construction instead of established query patterns, and doesn't follow the rich context building patterns established elsewhere.

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/lib.rs
**PRIORITY: LOW**
**FINDINGS:**

1. [Lines 7-11] **PATTERN: Missing module documentation**
   **CURRENT:** No doc comments on modules
   **SUGGESTION:** Add brief doc comments explaining what each service does
   **IMPACT:** maintainability
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/error.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. [Lines 8-33] **PATTERN: Manual Error enum duplicating project's sophisticated SinexError**
   **CURRENT:** Custom ServiceError enum with basic string messages
   **SUGGESTION:** Use SinexError from sinex-core which has rich context, categorization, serialization, HTTP status mapping, and retryability classification
   **IMPACT:** consistency|maintainability
   **EFFORT:** moderate

2. [Lines 11-14] **PATTERN: Manual From implementations available in SinexError**
   **CURRENT:** `#[from] sqlx::Error` and `#[from] serde_json::Error`
   **SUGGESTION:** SinexError already has these conversions with richer context
   **IMPACT:** consistency
   **EFFORT:** small

3. [Lines 17-26] **PATTERN: String-based error messages without rich context**
   **CURRENT:** Simple string messages like `NotFound(String)`, `InvalidInput(String)`
   **SUGGESTION:** Use SinexError's builder pattern (.with_context(), .with_operation(), .with_id())
   **IMPACT:** maintainability|readability
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/analytics.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. [Lines 26-48] **PATTERN: Duplicated time-range query logic with client-side filtering**
   **CURRENT:** Two similar branches doing client-side filtering for end_time
   **SUGGESTION:** Extract common query logic and apply filters systematically
   **IMPACT:** maintainability|readability
   **EFFORT:** small

2. [Line 41] **PATTERN: Magic timestamp value**
   **CURRENT:** `DateTime::from_timestamp(0, 0).unwrap()`
   **SUGGESTION:** Use named constant `const EPOCH_START: DateTime<Utc> = ...` or documented helper method
   **IMPACT:** readability
   **EFFORT:** trivial

3. [Lines 92-96, 144-148] **PATTERN: Repeated PgInterval construction**
   **CURRENT:** Verbose PgInterval struct construction repeated multiple times
   **SUGGESTION:** Create helper method `fn minutes_to_interval(minutes: i32) -> PgInterval`
   **IMPACT:** readability
   **EFFORT:** small

[✓ DONE] 4. [Lines 131-135] **PATTERN: Manual vector collection transformation**
   **CURRENT:** `for cmd_count in commands { result.push((cmd_count.command, cmd_count.count)); }`
   **SUGGESTION:** Use iterator methods: `commands.into_iter().map(|c| (c.command, c.count)).collect()`
   **IMPACT:** readability
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/content.rs
**PRIORITY: LOW**
**FINDINGS:**

[✓ DONE] 1. [Line 9] **PATTERN: Unused field with underscore prefix**
   **CURRENT:** `_pool: DbPool,`
   **SUGGESTION:** Either use the pool or remove it if truly unused
   **IMPACT:** maintainability
   **EFFORT:** trivial

2. [Lines 42-51] **PATTERN: Duplicate method implementation**
   **CURRENT:** `store_content` just delegates to `store_large_content` with identical signature
   **SUGGESTION:** Remove redundant method or add doc explaining the distinction
   **IMPACT:** maintainability
   **EFFORT:** trivial

3. [Lines 34-35, 58-59, 72-74] **PATTERN: Manual error wrapping without using SinexError context**
   **CURRENT:** `ServiceError::OperationFailed(format!("Blob storage failed: {}", e))`
   **SUGGESTION:** Use SinexError's .with_operation() or .with_context() for richer error context
   **IMPACT:** consistency
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/search.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. [Lines 41-111] **PATTERN: Manual SQL string construction with potential SQL injection risk**
   **CURRENT:** Dynamic SQL building with string concatenation and manual parameterization
   **SUGGESTION:** Use SeaQuery helpers available in project (seaquery_helpers::SeaQueryUlidExt) for type-safe query construction
   **IMPACT:** security|maintainability
   **EFFORT:** moderate

2. [Line 62] **PATTERN: Manual PostgreSQL array formatting**
   **CURRENT:** `params.push(format!("{{{}}}", query.sources.join(",")))`
   **SUGGESTION:** Use proper sqlx array parameter binding: `query.bind(&query.sources[..])`
   **IMPACT:** correctness|security
   **EFFORT:** small

3. [Lines 99-111] **PATTERN: Complex manual tuple destructuring**
   **CURRENT:** Verbose sqlx::query_as with complex tuple type and filter_map
   **SUGGESTION:** Use repository pattern methods or create dedicated query struct with FromRow
   **IMPACT:** readability|maintainability
   **EFFORT:** moderate

4. [Line 88] **PATTERN: Inefficient ILIKE text search**
   **CURRENT:** `payload::text ILIKE ${}` - inefficient for large datasets
   **SUGGESTION:** Use PostgreSQL full-text search or JSON operators for better performance
   **IMPACT:** performance
   **EFFORT:** moderate

5. [Lines 141-145] **PATTERN: Manual string slicing without UTF-8 safety**
   **CURRENT:** `&payload_str[start..end]` can panic on UTF-8 boundaries
   **SUGGESTION:** Use safe string truncation: `payload_str.char_indices().nth(150).map_or(payload_str, |(i, _)| &payload_str[..i])`
   **IMPACT:** correctness
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-services/src/pkm.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. [Lines 78-87] **PATTERN: Long match statement for entity type mapping**
   **CURRENT:** Verbose match with repeated CreateEntity calls
   **SUGGESTION:** Use `impl From<&str> for EntityType` or static HashMap for cleaner mapping
   **IMPACT:** readability
   **EFFORT:** small

2. [Lines 35, 89-93] **PATTERN: Repeated inline JSON metadata construction**
   **CURRENT:** `serde_json::json!({ "tags": tags, "created_at": chrono::Utc::now().to_rfc3339(), ... })`
   **SUGGESTION:** Extract into structured metadata builder or use serde Serialize struct
   **IMPACT:** readability|maintainability
   **EFFORT:** small

3. [Lines 253-261] **PATTERN: Manual checksum calculation duplication**
   **CURRENT:** Both blake3 and sha256 calculated manually
   **SUGGESTION:** Extract into `fn calculate_checksums(content: &[u8]) -> (String, String)` utility
   **IMPACT:** consistency
   **EFFORT:** trivial

4. [Lines 327-339, 354-364] **PATTERN: Manual JSON response construction**
   **CURRENT:** Verbose serde_json::json! macro with repeated field access
   **SUGGESTION:** Create `MaterialSummary` struct with serde derives for type safety and consistency
   **IMPACT:** maintainability|type safety
   **EFFORT:** small

5. [Lines 182-186] **PATTERN: Manual content preview generation without UTF-8 safety**
   **CURRENT:** Manual mime type checking and content slicing
   **SUGGESTION:** Extract into safe content preview helper with proper UTF-8 boundary handling
   **IMPACT:** readability|correctness
   **EFFORT:** small

---

**FOCUS PRIORITIES:**
1. **HIGH**: Replace custom ServiceError with project's sophisticated SinexError system
2. **HIGH**: Replace manual SQL construction in search.rs with SeaQuery builders  
3. **MEDIUM**: Extract common patterns (PgInterval, JSON responses, metadata building)
4. **LOW**: Clean up unused fields and duplicate methods

These improvements would align the services layer with the project's established architectural patterns while improving type safety, maintainability, and security.

## Agent 2: sinex-services Analysis

# Deep Micro-Refactoring Analysis Report

## ARCHITECTURAL CONTEXT:

**Key patterns observed:**
- Error handling using SatelliteError/SinexError with comprehensive error context and conversion traits
- Database access via SQLX with PostgreSQL, ULID primary keys, TimescaleDB for time-series data
- Event system using RawEvent with JSON payloads, schema validation, and provenance tracking
- Configuration management using validator crate with serde, environment variable parsing
- Testing infrastructure via sinex-test-utils with TestContext providing database isolation
- Async patterns using tokio channels, structured logging with tracing
- Type safety through NewType patterns for domain types (EventSource, EventType)
- Builder patterns implemented using bon derive macros

**Available utilities not being used optimally:**
- Extensive use of validator crate but some manual validation still exists
- color-eyre for error handling available but not always leveraged for context
- tokio utilities for timeout, retry patterns available but hand-rolled in some places
- itertools available in workspace but not used where beneficial

**Inconsistencies with project standards:**
- Mixed patterns for environment variable parsing (manual vs helper functions)
- Some error messages lack actionable context despite error infrastructure being available
- Inconsistent use of #[inline] hints for small functions
- Missing must_use attributes on important return types

---

## FILE: /realm/project/sinex/crate/lib/sinex-satellite-sdk/src/config.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Lines 259-275] PATTERN: String Allocation**
   **CURRENT:** Multiple `.to_string()` calls in environment parsing
   ```rust
   log_level: std::env::var("SINEX_LOG_LEVEL").unwrap_or_else(|_| default_log_level()),
   ingest_socket_path: std::env::var("SINEX_INGEST_SOCKET")
       .unwrap_or_else(|_| default_ingest_socket()),
   ```
   **SUGGESTION:** Create helper function to reduce duplication and improve readability
   **IMPACT:** readability|maintainability
   **EFFORT:** small

2. **[Lines 354-358] PATTERN: Complex Error Handling**
   **CURRENT:** Manual path handling with multiple fallbacks
   ```rust
   dirs::cache_dir()
       .map(|p| Utf8PathBuf::from_path_buf(p).unwrap_or_else(|_| Utf8PathBuf::from("/tmp")))
       .unwrap_or_else(|| Utf8PathBuf::from("/tmp"))
   ```
   **SUGGESTION:** Extract to named function with clear error handling
   **IMPACT:** readability|correctness
   **EFFORT:** small

3. **[Lines 390-394] PATTERN: Missing Error Context**
   **CURRENT:** Generic validation error without context
   ```rust
   validate_path(path)
       .map(|_| ())
       .map_err(|_| validator::ValidationError::new("invalid_socket_path"))
   ```
   **SUGGESTION:** Add specific error context about what makes the path invalid
   **IMPACT:** maintainability
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-satellite-sdk/src/grpc_client.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Lines 21-22] PATTERN: Unnecessary String Clones**
   **CURRENT:** Cloning strings for logging that could use references
   ```rust
   let socket_path_owned = socket_path.to_string();
   let socket_path_for_log = socket_path_owned.clone();
   ```
   **SUGGESTION:** Use single string variable or borrow where possible
   **IMPACT:** performance
   **EFFORT:** trivial

2. **[Lines 114-118] PATTERN: String Building**
   **CURRENT:** Multiple string conversions in protobuf conversion
   ```rust
   source: event.source.as_str().to_string(),
   event_type: event.event_type.as_str().to_string(),
   host: event.host.as_str().to_string(),
   ```
   **SUGGESTION:** Consider if protobuf accepts &str to avoid allocations
   **IMPACT:** performance
   **EFFORT:** small

3. **[Lines 47-52] PATTERN: Error Context**
   **CURRENT:** Basic error handling without context
   ```rust
   if inner.success {
       debug!("Successfully ingested event");
       Ok(inner.event_id.unwrap_or_default())
   } else {
       let error_msg = inner.error.unwrap_or_else(|| "Unknown error".to_string());
       error!("Failed to ingest event: {}", error_msg);
       Err(SatelliteError::Processing(error_msg))
   }
   ```
   **SUGGESTION:** Add event ID or other identifying context to error messages
   **IMPACT:** maintainability
   **EFFORT:** small

4. **[Lines 164] PATTERN: Missing Inline Hint**
   **CURRENT:** Small functions without inline hints
   ```rust
   pub fn new(client: IngestClient, batch_size: usize, timeout_secs: u64) -> Self
   ```
   **SUGGESTION:** Add #[inline] for small constructor functions
   **IMPACT:** performance
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-satellite-sdk/src/stream_processor.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Lines 232-237] PATTERN: Builder Pattern Repetition**
   **CURRENT:** Manual builder implementation for Checkpoint
   ```rust
   pub fn external(position: serde_json::Value, description: impl Into<String>) -> Self {
       Self::External {
           position,
           description: description.into(),
       }
   }
   ```
   **SUGGESTION:** Consider using bon::Builder derive for consistency
   **IMPACT:** consistency|maintainability
   **EFFORT:** moderate

2. **[Lines 674-675] PATTERN: Unused Parameter Warning Suppression**
   **CURRENT:** Manual parameter suppression
   ```rust
   let _ = (from, until, args);
   ```
   **SUGGESTION:** Use #[allow(unused_variables)] attribute instead for clarity
   **IMPACT:** readability
   **EFFORT:** trivial

3. **[Lines 439] PATTERN: TODO Comments**
   **CURRENT:** TODO comment about macro usage
   ```rust
   // TODO: Fix macro to use sinex_core instead of sinex_db
   ```
   **SUGGESTION:** Either fix the macro or file an issue and reference it
   **IMPACT:** maintainability
   **EFFORT:** small

4. **[Lines 1048-1051] PATTERN: Error Handling**
   **CURRENT:** Panicking on dummy client creation
   ```rust
   let ingest_client = IngestClient::new("/dev/null")
       .await
       .unwrap_or_else(|_| panic!("Failed to create dummy ingest client"));
   ```
   **SUGGESTION:** Return proper error instead of panicking
   **IMPACT:** correctness
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-satellite-sdk/src/cli.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Lines 138-150] PATTERN: Complex Match Logic**
   **CURRENT:** Nested parsing logic in match
   ```rust
   match checkpoint_str.to_lowercase().as_str() {
       "none" | "start" => Ok(Checkpoint::None),
       _ => {
           if let Ok(value) = serde_json::from_str::<serde_json::Value>(checkpoint_str) {
               serde_json::from_value::<Checkpoint>(value).context("Invalid checkpoint JSON")
           } else if let Ok(timestamp) = checkpoint_str.parse::<DateTime<Utc>>() {
               Ok(Checkpoint::timestamp(timestamp, None))
           } else {
               Ok(Checkpoint::stream(checkpoint_str, None))
           }
   ```
   **SUGGESTION:** Extract parsing logic to separate functions for each format
   **IMPACT:** readability|maintainability
   **EFFORT:** small

2. **[Lines 138] PATTERN: Unnecessary String Allocation**
   **CURRENT:** `checkpoint_str.to_lowercase().as_str()`
   **SUGGESTION:** Use str methods directly or `matches!` macro for simple comparisons
   **IMPACT:** performance
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-macros/src/error_context.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Lines 46] PATTERN: Large Function**
   **CURRENT:** The with_context function is over 350 lines
   **SUGGESTION:** Extract validation logic, context building, and code generation into separate functions
   **IMPACT:** maintainability|readability
   **EFFORT:** moderate

2. **[Lines 248-253] PATTERN: Warning Suppression**
   **CURRENT:** Manual eprintln! for warnings
   ```rust
   if !suppress_warnings {
       eprintln!(
           "warning: unknown attribute '{}' in with_context macro",
           path.get_ident()
               .map(|i| i.to_string())
               .unwrap_or_else(|| "<unknown>".to_string())
       );
   }
   ```
   **SUGGESTION:** Use proper diagnostic reporting for proc macros
   **IMPACT:** maintainability
   **EFFORT:** small

[✓ DONE] 3. **[Lines 339-340] PATTERN: Unused Variable**
   **CURRENT:** `let _final_logic = execution_logic.clone();`
   **SUGGESTION:** Remove unused variable or document why it exists
   **IMPACT:** readability
   **EFFORT:** trivial

---

## FILE: /realm/project/sinex/crate/lib/sinex-test-utils/src/lib.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Lines 721] PATTERN: Error Conversion**
   **CURRENT:** Manual error conversion in test
   ```rust
   let ctx = TestContext::with_name(&format!("concurrent_{}", i))
       .await
       .map_err(|e| SinexError::unknown(e.to_string()))?;
   ```
   **SUGGESTION:** Implement proper error conversion trait instead
   **IMPACT:** maintainability
   **EFFORT:** small

2. **[Lines 790] PATTERN: Error Message Quality**
   **CURRENT:** Generic error assertion
   ```rust
   assert!(result.unwrap_err().to_string().contains("source"));
   ```
   **SUGGESTION:** Test for specific error types or use structured error assertions
   **IMPACT:** maintainability
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-test-utils/src/builders.rs
**PRIORITY: LOW**
**FINDINGS:**

1. **[Lines 66-77] PATTERN: Builder Inconsistency**
   **CURRENT:** Manual builder implementation while bon::Builder is available
   ```rust
   let processor_name = ProcessorName::new(&self.processor_name);
   let group = ConsumerGroup::new(
       &self
           .consumer_group
           .unwrap_or_else(|| format!("{}-group", self.processor_name)),
   );
   ```
   **SUGGESTION:** Use bon::Builder derive for consistency with other builders
   **IMPACT:** consistency
   **EFFORT:** small

---

## FILE: /realm/project/sinex/crate/lib/sinex-migrations/src/lib.rs
**PRIORITY: LOW**
**FINDINGS:**

1. **[Lines 28-49] PATTERN: Large Vec Construction**
   **CURRENT:** Manual vector construction with many Box::new calls
   ```rust
   vec![
       Box::new(m20240101_000001_initial_schema::Migration),
       Box::new(m20240102_000002_add_validation_functions::Migration),
       // ... 17 more entries
   ]
   ```
   **SUGGESTION:** Consider using a macro to reduce boilerplate
   **IMPACT:** maintainability
   **EFFORT:** small

---

## SUMMARY RECOMMENDATIONS:

**High Priority (3 files):**
- Extract complex logic in error_context.rs into smaller functions
- Fix panic-inducing error handling in stream_processor.rs
- Add proper error context and inline hints in grpc_client.rs

**Medium Priority (4 files):**
- Create helper functions for environment variable parsing patterns
- Improve error messages with actionable context
- Extract complex parsing logic into dedicated functions

**Low Priority (2 files):**
- Consistency improvements with builder patterns
- Macro opportunities for reducing boilerplate

**Total Issues Identified:** 17 micro-refactoring opportunities
**Estimated Impact:** Primarily readability and maintainability improvements with some performance gains
**Implementation Effort:** Mostly small to moderate changes, no major architectural modifications required

## Agent 3: SDK and migrations for refactoring

Now I have a comprehensive understanding of the codebase architecture and patterns. Let me perform my systematic analysis:

## ARCHITECTURAL CONTEXT

Based on my analysis of the project architecture and established patterns:

**Key patterns observed:**
- Comprehensive error handling using `SinexError` with rich context and builder pattern
- Database access via SQLX with typed queries and ULID-based primary keys
- Event validation using JSON Schema with in-memory caching
- Async/await throughout with proper tokio integration
- Configuration validation using the validator crate and `bon` builder pattern
- Structured logging with tracing and telemetry systems
- Transactional outbox pattern for reliable message publishing
- Extensive use of Arc for shared state management
- Testing with `sinex_test` macro and comprehensive test coverage

**Available utilities being used:**
- `sinex-core` types (`SinexError`, `Ulid`, domain types)
- `sinex-satellite-sdk` for checkpoint management
- Telemetry and metrics systems
- Database helpers and query utilities
- JSON schema validation infrastructure

**Project standards observed:**
- ULID-based IDs for all entities
- Structured error handling with context
- Batch processing for database operations
- Graceful shutdown patterns
- Configuration validation and environment variable support

## FILE-BY-FILE ANALYSIS

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/lib.rs**
PRIORITY: MEDIUM
FINDINGS:

1. [Line 26-57] PATTERN: Custom error enum with redundant variants
   CURRENT: Separate `IngestdError` when project uses `SinexError`
   SUGGESTION: Use project's unified `SinexError` instead of custom error type
   IMPACT: consistency|maintainability
   EFFORT: small

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/main.rs**
PRIORITY: LOW
FINDINGS:

1. [Line 82-94] PATTERN: Manual validation logic in main
   CURRENT: Inline validation with process::exit calls
   SUGGESTION: Extract validation to config method, use ? operator for cleaner error propagation
   IMPACT: readability
   EFFORT: trivial

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/config.rs**
PRIORITY: MEDIUM
FINDINGS:

1. [Line 81-103] PATTERN: Manual builder pattern when bon is available
   CURRENT: Custom `from_args` method with manual field assignment
   SUGGESTION: Use bon builder pattern consistently with the rest of the struct
   IMPACT: consistency|maintainability
   EFFORT: small

2. [Line 232-237] PATTERN: Manual URL validation instead of using existing utilities
   CURRENT: String prefix checking
   SUGGESTION: Use `url` crate parsing which is already available in workspace
   IMPACT: correctness|robustness
   EFFORT: trivial

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/service.rs**
PRIORITY: HIGH
FINDINGS:

1. [Line 39-40] PATTERN: Static allocation when constants could be used
   CURRENT: `Lazy<String>` for version
   SUGGESTION: Use const or get from Cargo.toml at compile time
   IMPACT: performance|memory
   EFFORT: trivial

2. [Line 628-686] PATTERN: Complex raw SQL with many binds
   CURRENT: 17 separate bind parameters for UNNEST query
   SUGGESTION: Extract to a reusable database helper with typed parameters
   IMPACT: maintainability|readability
   EFFORT: moderate

3. [Line 296-347] PATTERN: Complex async task with nested select
   CURRENT: Deeply nested tokio::select! for shutdown handling
   SUGGESTION: Extract shutdown signal handling to a utility function
   IMPACT: readability
   EFFORT: small

4. [Line 520-523] PATTERN: Manual error counting
   CURRENT: Manual atomic increments for error stats
   SUGGESTION: Use project's telemetry system consistently
   IMPACT: consistency
   EFFORT: small

5. [Line 43] PATTERN: Type alias without descriptive naming
   CURRENT: `SubjectCache` as mutex-wrapped hash map
   SUGGESTION: Make this a proper newtype with methods for cache operations
   IMPACT: readability|maintainability
   EFFORT: small

6. [Line 671-677] PATTERN: Hardcoded column defaults in query
   CURRENT: `vec![None; events.len()]` for schema columns
   SUGGESTION: Use actual schema values from event validation
   IMPACT: correctness
   EFFORT: moderate

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/validator.rs**
PRIORITY: MEDIUM
FINDINGS:

1. [Line 37-42] PATTERN: Complex nested generic types
   CURRENT: `Arc<parking_lot::RwLock<AHashMap<Arc<String>, SchemaCacheEntry>>>`
   SUGGESTION: Create newtype wrapper for the cache with descriptive methods
   IMPACT: readability|maintainability
   EFFORT: small

2. [Line 25-32] PATTERN: Manual cache entry struct when simpler approach available
   CURRENT: Multiple Arc-wrapped strings in cache entry
   SUGGESTION: Use interned strings or string IDs to reduce memory allocations
   IMPACT: performance|memory
   EFFORT: moderate

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/figment_config.rs**
PRIORITY: MEDIUM
FINDINGS:

1. [Line 83-113] PATTERN: Separate default functions instead of using defaults
   CURRENT: Individual `default_*` functions
   SUGGESTION: Use serde default attributes directly with const values
   IMPACT: maintainability|readability
   EFFORT: trivial

2. [Line 136-163] PATTERN: Duplicate figment setup logic
   CURRENT: `load()` and `load_from()` methods with similar logic
   SUGGESTION: Extract common figment setup to helper method
   IMPACT: maintainability
   EFFORT: trivial

3. [Line 198-224] PATTERN: Custom validation when validator crate has built-ins
   CURRENT: Manual URL and path validation
   SUGGESTION: Use validator crate's built-in validators where possible
   IMPACT: consistency|robustness
   EFFORT: small

**FILE: /realm/project/sinex/crate/core/sinex-ingestd/src/schema_sync.rs**
PRIORITY: LOW  
FINDINGS:

1. [Line 41-55] PATTERN: Manual counting in loop
   CURRENT: Separate counters incremented in match arms
   SUGGESTION: Use iterator methods to count results
   IMPACT: readability
   EFFORT: trivial

2. [Line 46-48] PATTERN: Computing hash on every iteration
   CURRENT: `compute_content_hash(&schema_content)` called inline
   SUGGESTION: Pre-compute hashes or cache them if expensive
   IMPACT: performance
   EFFORT: small

## SUMMARY

The ingestd crate shows good architectural alignment with the project's patterns but has several opportunities for micro-improvements:

**High Priority:**
- Simplify the complex UNNEST query in service.rs by extracting to a reusable helper
- Use project's unified SinexError instead of custom error enum

**Medium Priority:**
- Create newtype wrappers for complex generic cache types in validator.rs
- Use consistent builder patterns with bon throughout
- Extract common configuration logic and use standard validators

**Low Priority:**
- Replace static allocations with compile-time constants
- Use iterator methods instead of manual counting
- Clean up default value functions

The code generally follows good Rust practices but could benefit from better alignment with the project's established utilities and patterns. Most issues are localized improvements that don't require cross-module changes.

## Agent 4: Analyze sinex-ingestd for refactoring

Now I have enough information to perform a systematic analysis. Let me create my analysis report:

## ARCHITECTURAL CONTEXT:

**Key Patterns Observed:**
- **Error Handling**: Consistent use of SinexError with rich context through `.with_context()` and `.with_operation()` methods
- **ULID Usage**: ULIDs are the primary key type with explicit conversion functions (`ulid_to_uuid`, `uuid_to_ulid`) 
- **Database Patterns**: SQLX with async/await, explicit error handling via `db_error()` helper function
- **Testing**: Modern test infrastructure using `#[sinex_test]` macro and sinex-test-utils
- **Service Architecture**: Arc<Service> pattern for shared service instances in containers
- **Structured Logging**: Consistent use of `tracing` crate with info!, debug!, warn!, error! macros
- **JSON-RPC Protocol**: Standard JSON-RPC 2.0 implementation patterns

**Available Utilities Not Being Used:**
- `db_error()` helper function for consistent SQLX error conversion
- `with_transaction()` helper for simplified transaction handling
- ULID conversion helpers from query_helpers module
- SinexError's rich context methods (`.with_path()`, `.with_duration()`, `.with_count()`, `.with_id()`)
- Project's established Result extension traits (`ResultExt`)

**Inconsistencies with Project Standards:**
- Manual error conversions instead of using established `db_error()` helper
- Some missing inline documentation for complex business logic
- Inconsistent use of established error context patterns

---

## DETAILED FILE ANALYSIS:

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/main.rs**
**PRIORITY: LOW**
**FINDINGS:**

1. **[Line 56-64] PATTERN: Tracing initialization**
   CURRENT: Manual tracing setup with unwrap_or_else
   SUGGESTION: Use project's established tracing patterns or extract to helper function
   IMPACT: consistency
   EFFORT: trivial

2. **[Line 78-79] PATTERN: Error handling**
   CURRENT: Using `?` operator without additional context
   SUGGESTION: Add operation context using `.with_operation("start_rpc_server")`
   IMPACT: maintainability
   EFFORT: trivial

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/service_container.rs**
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 28-30] PATTERN: Error handling consistency**
   CURRENT: Using `wrap_err` from color_eyre
   SUGGESTION: Use SinexError::configuration() with project's context patterns
   IMPACT: consistency
   EFFORT: small

2. **[Line 33-35] PATTERN: Database error handling**
   CURRENT: Manual error conversion with `wrap_err`
   SUGGESTION: Use established `db_error()` helper from query_helpers
   IMPACT: consistency|maintainability
   EFFORT: trivial

3. **[Line 43-44] PATTERN: File system error handling**
   CURRENT: Using `with_context` with formatted string
   SUGGESTION: Use SinexError::io().with_path() for better structured context
   IMPACT: consistency|readability
   EFFORT: trivial

[✓ DONE] 4. **[Line 96-98] PATTERN: Magic numbers**
   CURRENT: Hardcoded telemetry interval (300 seconds)
   SUGGESTION: Extract to named constant or configuration parameter
   IMPACT: maintainability
   EFFORT: trivial

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/handlers.rs**
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Line 46-52] PATTERN: Verbose error handling**
   CURRENT: Verbose unwrap chain with manual conversion
   SUGGESTION: Extract ULID parsing to helper function or use combinator pattern
   IMPACT: readability|maintainability
   EFFORT: small

2. **[Line 198] PATTERN: Unsafe string conversion**
   CURRENT: `unwrap_or_else(|_| "<binary content>".to_string())`
   SUGGESTION: Use proper error handling or encoding detection
   IMPACT: correctness|robustness
   EFFORT: small

3. **[Line 15-29] PATTERN: Duplicate parameter extraction**
   CURRENT: Repeated pattern of extracting and parsing parameters
   SUGGESTION: Create a generic parameter extraction helper or macro
   IMPACT: maintainability|readability
   EFFORT: moderate

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/rpc_server.rs**
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 154-156] PATTERN: Unix socket handling**
   CURRENT: Removes existing socket but then binds to TCP
   SUGGESTION: Either implement actual Unix socket or remove socket handling code
   IMPACT: correctness|clarity
   EFFORT: small

2. **[Line 174-175] PATTERN: Hardcoded address**
   CURRENT: Hardcoded TCP address "127.0.0.1:9999"
   SUGGESTION: Make configurable through CLI parameter or environment variable
   IMPACT: maintainability
   EFFORT: small

3. **[Line 84-126] PATTERN: Large match statement**
   CURRENT: Large match statement with duplicate method dispatch logic
   SUGGESTION: Extract to dispatch table or use handler registry pattern
   IMPACT: maintainability|readability
   EFFORT: moderate

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/native_messaging.rs**
**PRIORITY: MEDIUM**
**FINDINGS:**

[✓ DONE] 1. **[Line 66-69] PATTERN: Magic number**
   CURRENT: Hardcoded 1MB message limit
   SUGGESTION: Extract to named constant or configuration
   IMPACT: maintainability
   EFFORT: trivial

2. **[Line 132-161] PATTERN: Duplicate dispatch logic**
   CURRENT: Identical dispatch logic to RPC server
   SUGGESTION: Extract common dispatch logic to shared module
   IMPACT: maintainability|consistency
   EFFORT: small

3. **[Line 3] PATTERN: Dependency optimization**
   CURRENT: Using byteorder crate for simple endian operations
   SUGGESTION: Use native Rust methods (u32::to_ne_bytes(), u32::from_ne_bytes())
   IMPACT: performance|dependency
   EFFORT: trivial

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/cascade_analyzer.rs**
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Line 166-169] PATTERN: Manual ULID/UUID conversion**
   CURRENT: Manual conversion using `ulid_to_uuid(*id).into()`
   SUGGESTION: Use established UlidArrayExt trait: `event_ids.to_uuid_vec()`
   IMPACT: consistency|readability
   EFFORT: trivial

2. **[Line 316-318] PATTERN: Unused type annotation**
   CURRENT: `sqlx::query_as::<_, (Ulid, Ulid)>(&query)` but using `(uuid::Uuid, uuid::Uuid)` in database
   SUGGESTION: Fix type annotation to match actual database types or use proper ULID casting
   IMPACT: correctness
   EFFORT: small

3. **[Line 99] PATTERN: ULID usage for temporary names**
   CURRENT: Using Ulid for temp table names
   SUGGESTION: Use simpler random identifier or timestamp-based names for temp resources
   IMPACT: performance
   EFFORT: trivial

4. **[Line 240-245] PATTERN: Missing error context**
   CURRENT: Query operations without contextual error information
   SUGGESTION: Add context using `db_error()` helper and `.with_operation()` pattern
   IMPACT: maintainability|debugging
   EFFORT: small

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/replay_state_machine.rs**
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 45-92] PATTERN: Complex state validation**
   CURRENT: Large nested match statement for state transitions
   SUGGESTION: Consider using a state transition table or state machine library
   IMPACT: maintainability
   EFFORT: moderate

2. **[Line 497-501] PATTERN: Manual byte manipulation**
   CURRENT: Manual ULID byte extraction for lock ID
   SUGGESTION: Extract to helper function or use ULID's built-in hash methods
   IMPACT: readability|maintainability
   EFFORT: small

3. **[Line 208-223] PATTERN: Direct SQLX without error helpers**
   CURRENT: Using sqlx directly without established error handling patterns
   SUGGESTION: Use `db_error()` helper for consistent error conversion
   IMPACT: consistency
   EFFORT: trivial

---

**FILE: /realm/project/sinex/crate/core/sinex-gateway/src/lib.rs**
**PRIORITY: LOW**
**FINDINGS:**

1. **[Line 6-8] PATTERN: Missing native_messaging module**
   CURRENT: Not exposing native_messaging module
   SUGGESTION: Consider exposing for testing or external use if needed
   IMPACT: testing|modularity
   EFFORT: trivial

---

## SUMMARY:

**High Priority Issues:**
- Manual ULID/UUID conversions not using established helpers (cascade_analyzer.rs)
- Unsafe string conversion from binary data (handlers.rs)
- Verbose parameter extraction patterns repeated across handlers (handlers.rs)

**Medium Priority Issues:**
- Inconsistent error handling patterns not using project's established helpers
- Duplicate dispatch logic between RPC and native messaging
- Hardcoded configuration values that should be configurable

**Low Priority Issues:**
- Missing operation context in some error paths
- Minor dependency optimizations
- Module exposure for testing

The gateway crate generally follows good Rust practices but could benefit from greater consistency with the project's established patterns, particularly around error handling and ULID conversion utilities.

## Agent 5: Analyze sinex-gateway for refactoring

## PHASE 1: ARCHITECTURAL UNDERSTANDING - COMPLETE

**ARCHITECTURAL CONTEXT:**

Key patterns observed:
- **ULID-based IDs**: Time-ordered, distributed-safe primary keys throughout
- **Event-driven architecture**: Immutable events in `core.events` with comprehensive provenance tracking
- **StatefulStreamProcessor trait**: Unified interface for both ingestors and automata
- **Error handling**: SinexError with rich context, thiserror for custom types, color-eyre for reporting
- **Database patterns**: SQLX with offline mode, repository pattern via DbPoolExt
- **Testing**: rstest, proptest, insta, sinex-test-utils with parallel execution
- **Configuration**: figment with TOML/JSON/env support, camino for UTF-8 paths

Available utilities not being used:
- `once_cell` for static initialization (some manual lazy_static usage found)
- `itertools` for iterator combinators (manual loops where combinators could help)
- `bon` builder pattern (manual struct construction in places)
- `regex` crate more efficiently (compilation happening repeatedly)
- `which` crate utilities for command existence checks

Inconsistencies with project standards:
- Some manual error string formatting instead of using SinexError context
- Inconsistent use of camino::Utf8PathBuf vs std::path::PathBuf
- Some .unwrap() usage where .expect() with context would be better
- Mixed naming conventions (get_foo vs foo for getters)

## PHASE 2: SYSTEMATIC ANALYSIS - FINDINGS

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/shell_detection.rs
**PRIORITY: MEDIUM**

**FINDINGS:**

1. **[Line 44, 59]** PATTERN: Duplicate home directory retrieval
   CURRENT: `dirs::home_dir().and_then(|p| Utf8PathBuf::from_path_buf(p).ok())` repeated
   SUGGESTION: Extract to helper method `get_home_dir() -> Option<Utf8PathBuf>`
   IMPACT: readability|maintainability
   EFFORT: trivial

2. **[Line 179]** PATTERN: Inefficient command existence check
   CURRENT: `which::which(cmd).is_ok()`
   SUGGESTION: Use `which::which(cmd).is_ok()` but cache results for performance
   IMPACT: performance
   EFFORT: small

3. **[Line 192-200]** PATTERN: Verbose command output parsing
   CURRENT: Long chain of `.ok()` and `.and_then()` calls
   SUGGESTION: Use `?` operator in helper function with proper error handling
   IMPACT: readability
   EFFORT: small

4. **[Line 207-217]** PATTERN: Platform-specific code could be cleaner
   CURRENT: `#[cfg(unix)]` block with manual parsing
   SUGGESTION: Use `sysinfo` crate which is already available in dependencies
   IMPACT: correctness|maintainability
   EFFORT: moderate

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/kitty.rs
**PRIORITY: HIGH**

**FINDINGS:**

1. **[Line 128-135]** PATTERN: Manual socket path construction
   CURRENT: Manual string formatting for socket candidates
   SUGGESTION: Use iterator combinators and extract to helper method
   IMPACT: readability|maintainability
   EFFORT: small

2. **[Line 157-200]** PATTERN: Repetitive error handling in socket communication
   CURRENT: Multiple `.map_err(|e| SatelliteError::Processing(format!(...)))` patterns
   SUGGESTION: Create helper function `io_to_satellite_error` or use `with_context`
   IMPACT: readability|consistency
   EFFORT: trivial

3. **[Line 202-216]** PATTERN: Complex JSON extraction from framed response
   CURRENT: Manual string searching for JSON boundaries
   SUGGESTION: Extract to helper method `extract_json_from_framed_response`
   IMPACT: readability|maintainability
   EFFORT: small

4. **[Line 229-284]** PATTERN: Deep nested JSON parsing with tuple destructuring
   CURRENT: Complex nested pattern matching
   SUGGESTION: Use serde_json::from_value with proper struct types
   IMPACT: correctness|readability
   EFFORT: moderate

5. **[Line 417-434]** PATTERN: Manual loop for command extraction
   CURRENT: Manual iteration with nested loops
   SUGGESTION: Use iterator combinators `lines().rev().find_map()`
   IMPACT: performance|readability
   EFFORT: trivial

6. **[Line 565-581]** PATTERN: Duplicate duration elapsed check
   CURRENT: Same `elapsed().unwrap_or(Duration::ZERO) >= scrollback_interval` pattern repeated
   SUGGESTION: Extract to helper method or use a single check with boolean flag
   IMPACT: maintainability
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/unified_processor.rs
**PRIORITY: HIGH**

**FINDINGS:**

1. **[Line 54-82]** PATTERN: Default implementation could use builder pattern
   CURRENT: Manual construction of TerminalConfig with verbose HashMap initialization
   SUGGESTION: Use `bon` builder pattern or implement builder manually
   IMPACT: readability
   EFFORT: small

2. **[Line 204-233]** PATTERN: File existence check with metadata repeated
   CURRENT: Manual fs operations repeated for multiple file types
   SUGGESTION: Extract to helper method `get_file_metadata(path) -> Option<FileMetadata>`
   IMPACT: readability|maintainability
   EFFORT: trivial

3. **[Line 237-253]** PATTERN: Rough estimate calculations
   CURRENT: `db_size_bytes / 100` and similar magic numbers
   SUGGESTION: Use named constants for estimation factors
   IMPACT: maintainability
   EFFORT: trivial

4. **[Line 514-577]** PATTERN: Configuration parsing with repeated error handling
   CURRENT: Multiple serde_json::from_value calls with same error pattern
   SUGGESTION: Create helper macro or function for config field parsing
   IMPACT: readability|consistency
   EFFORT: small

5. **[Line 773-787]** PATTERN: File size estimation logic duplicated
   CURRENT: Same file reading and line counting logic repeated
   SUGGESTION: Extract to helper method `estimate_file_entries(path) -> u64`
   IMPACT: maintainability|performance
   EFFORT: small

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-command-canonicalizer/src/unified_processor.rs
**PRIORITY: MEDIUM**

**FINDINGS:**

1. **[Line 40-56]** PATTERN: Manual JSON value extraction trait
   CURRENT: Custom JsonExtractor trait with repetitive implementations
   SUGGESTION: Use serde_json built-in methods or create a more robust helper
   IMPACT: maintainability
   EFFORT: small

2. **[Line 263-286]** PATTERN: String literal array for event sources
   CURRENT: Hardcoded array of source strings
   SUGGESTION: Use constants or enum with Into<&str> implementation
   IMPACT: maintainability
   EFFORT: trivial

3. **[Line 146-150]** PATTERN: Unsafe ULID access
   CURRENT: `.map(|id| id.as_ulid().clone()).unwrap_or_else(|| Ulid::new())`
   SUGGESTION: Use proper error handling or ensure ID is always present
   IMPACT: correctness
   EFFORT: small

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/atuin.rs
**PRIORITY: LOW**

**FINDINGS:**

1. **[Line 31-38]** PATTERN: Environment variable handling
   CURRENT: `std::env::var("HOME").unwrap_or_else(|_| "/home/user".to_string())`
   SUGGESTION: Use `dirs::home_dir()` which is already imported elsewhere
   IMPACT: consistency|correctness
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-command-canonicalizer/src/main_old.rs
**PRIORITY: HIGH**

**FINDINGS:**

1. **[Entire file]** PATTERN: Obsolete legacy file
   CURRENT: Old implementation that's no longer used
   SUGGESTION: Remove file entirely - it serves no purpose and creates confusion
   IMPACT: maintainability
   EFFORT: trivial

### FILES: main_old_nats.rs, main_unified.rs (similar obsolete files)
**PRIORITY: HIGH**

**FINDINGS:**

1. **[Entire files]** PATTERN: Legacy files not in active use
   CURRENT: Multiple old implementation files
   SUGGESTION: Remove obsolete files to reduce codebase confusion
   IMPACT: maintainability
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/history.rs
**PRIORITY: MEDIUM**

**FINDINGS:**

1. **[Line 70-80]** PATTERN: String content handling for large files
   CURRENT: `fs::read_to_string()` loads entire file into memory
   SUGGESTION: Use buffered reading for large history files to avoid memory issues
   IMPACT: performance|correctness
   EFFORT: moderate

## SUMMARY RECOMMENDATIONS

**IMMEDIATE ACTIONS (High Impact/Low Effort):**
[✓ DONE] 1. Remove obsolete files (main_old.rs, main_old_nats.rs, main_unified.rs)
2. Extract repetitive error handling patterns in kitty.rs
3. Add named constants for magic numbers in estimation logic
4. Use iterator combinators instead of manual loops where appropriate

**CODE QUALITY IMPROVEMENTS (Medium Priority):**
1. Extract common file metadata operations to helper functions
2. Implement builder pattern for complex configuration structs
3. Use sysinfo crate for platform-specific operations
4. Create configuration parsing helpers to reduce repetition

**PERFORMANCE OPTIMIZATIONS (Lower Priority):**
1. Cache command existence checks in shell detection
2. Use buffered file reading for large history files
3. Optimize JSON parsing with proper struct types instead of manual extraction

The codebase shows good architectural consistency and follows most project patterns well. The main opportunities are in reducing code duplication, improving error handling consistency, and removing obsolete files.

## Agent 6: Analyze terminal satellites for refactoring

## ARCHITECTURAL CONTEXT

**Key patterns observed:**
- **Error handling**: Rich SinexError with context chaining, Result types throughout, thiserror/color_eyre integration
- **Database patterns**: SQLX compile-time verification, ULID primary keys, time-ordered distributed-safe IDs
- **Event patterns**: Immutable RawEvent types, JSON schema validation, comprehensive provenance tracking
- **Testing patterns**: sinex_test macros, rstest for parameterized tests, proptest for property testing
- **Path handling**: camino for UTF-8 paths, validate_path utilities, SanitizedPath domain types
- **Async patterns**: Tokio throughout, async-trait for traits, structured concurrency
- **Configuration**: Serde serialization, figment for config management, environment variable integration
- **Logging**: Tracing with structured fields, context propagation
- **Macros**: Heavy use of procedural macros for boilerplate reduction

**Available utilities not being leveraged:**
- sinex-core path validation utilities (validate_path, SanitizedPath)
- Rich SinexError context building methods (with_context, with_operation, with_path)
- sinex-test-utils comprehensive test infrastructure
- with_context attribute macro for operation tracing
- sinex-core domain types and validation

**Inconsistencies with project standards:**
- Mixed use of color_eyre::eyre::Result vs sinex_core::types::error::Result
- Some manual error conversions instead of using established patterns
- Inconsistent structured logging adoption
- Some TODO placeholders in production code paths

---

## SYSTEMATIC ANALYSIS FINDINGS

### FILE: /realm/project/sinex/crate/satellites/sinex-fs-watcher/src/unified_processor.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Line 278-283] PATTERN: Missing must_use attribute**
   **CURRENT:** `async fn take_snapshot(&mut self) -> SatelliteResult<FilesystemState>`
   **SUGGESTION:** Add `#[must_use = "Snapshot result should be used or stored"]`
   **IMPACT:** correctness | **EFFORT:** trivial

2. **[Line 429] PATTERN: Complex boolean expression**
   **CURRENT:** `if !self.matches_patterns(utf8_path) { continue; }`
   **SUGGESTION:** Extract to `let should_process = self.matches_patterns(utf8_path); if !should_process { continue; }`
   **IMPACT:** readability | **EFFORT:** trivial

3. **[Line 452-481] PATTERN: Complex nested function needing extraction**
   **CURRENT:** `matches_patterns` method with complex nested logic
   **SUGGESTION:** Extract pattern matching logic into separate methods: `matches_watch_patterns()`, `matches_ignore_patterns()`
   **IMPACT:** readability | **EFFORT:** small

4. **[Line 585-590] PATTERN: Hardcoded error strings**
   **CURRENT:** `"Failed to create debouncer: {}"`
   **SUGGESTION:** Define error message constants at module level
   **IMPACT:** maintainability | **EFFORT:** trivial

5. **[Line 613-621] PATTERN: Clone-heavy temporary instance creation**
   **CURRENT:** `let temp_processor = FilesystemProcessor { config: config.clone(), watch_roots: watch_roots.clone(), ... }`
   **SUGGESTION:** Pass required data as parameters instead of cloning entire processor
   **IMPACT:** performance | **EFFORT:** small

6. **[Line 787-791] PATTERN: Placeholder implementation**
   **CURRENT:** `fn convert_fs_event(...) -> SatelliteResult<Vec<RawEvent>> { Ok(vec![]) }`
   **SUGGESTION:** Either implement or mark with proper TODO with tracking issue
   **IMPACT:** correctness | **EFFORT:** small

7. **[Line 870-881] PATTERN: Complex initialization logic in trait method**
   **CURRENT:** Long initialization logic in `initialize()` method
   **SUGGESTION:** Extract to private `configure_watch_patterns()` method
   **IMPACT:** readability | **EFFORT:** small

8. **[Line 1056] PATTERN: Inefficient iterator usage**
   **CURRENT:** `&self.watch_roots.iter().map(|p| p.to_string()).collect()`
   **SUGGESTION:** Use `&self.watch_roots.iter().map(|p| p.as_str()).collect::<Vec<_>>()`
   **IMPACT:** performance | **EFFORT:** trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-fs-watcher/src/cli.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. [✓ DONE] **[Line 61] PATTERN: TODO in production code**
   **CURRENT:** `todo!("Direct mode not fully implemented in CLI")`
   **SUGGESTION:** Return proper error with context: `Err(color_eyre::eyre::eyre!("Direct mode not yet supported"))`
   **IMPACT:** correctness | **EFFORT:** trivial

2. **[Line 50-89] PATTERN: Long match arms**
   **CURRENT:** Complex match arms handling different command types
   **SUGGESTION:** Extract to separate functions: `run_direct_mode()`, `run_sensd_mode()`
   **IMPACT:** readability | **EFFORT:** small

3. **[Line 72-80] PATTERN: Manual struct building**
   **CURRENT:** Manual field assignments to config
   **SUGGESTION:** Use builder pattern or implement From trait for Args -> SensdIntegrationConfig
   **IMPACT:** maintainability | **EFFORT:** small

### FILE: /realm/project/sinex/crate/satellites/sinex-system-satellite/src/lib.rs  
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 35-70] PATTERN: Manual Default implementation**
   **CURRENT:** Manual Default implementation for SystemConfig
   **SUGGESTION:** Use `#[derive(Default)]` with `#[default]` field attributes
   **IMPACT:** maintainability | **EFFORT:** trivial

2. **[Line 74-87] PATTERN: Custom error type without context**
   **CURRENT:** Basic thiserror implementation
   **SUGGESTION:** Add context methods like the established SinexError pattern
   **IMPACT:** consistency | **EFFORT:** small

3. **[Line 89-93] PATTERN: Simple error conversion**
   **CURRENT:** Manual From implementation
   **SUGGESTION:** Use `#[from]` attribute in error enum
   **IMPACT:** maintainability | **EFFORT:** trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-system-satellite/src/unified_processor.rs
**PRIORITY: HIGH**  
**FINDINGS:**

1. **[Line 112-125] PATTERN: Repetitive struct initialization**
   **CURRENT:** Manual field initialization in `new()`
   **SUGGESTION:** Use `Self { field: default_value(), ..Default::default() }`
   **IMPACT:** maintainability | **EFFORT:** trivial

2. **[Line 142-147] PATTERN: Mutable variable accumulation**
   **CURRENT:** Multiple mutable variables being built up
   **SUGGESTION:** Use builder pattern or collect into structure first
   **IMPACT:** readability | **EFFORT:** small

3. **[Line 150+] PATTERN: Missing implementation**
   **CURRENT:** File appears truncated, likely incomplete implementation
   **SUGGESTION:** Complete implementation or add proper placeholder with tracking
   **IMPACT:** correctness | **EFFORT:** moderate

### FILE: /realm/project/sinex/crate/satellites/sinex-system-satellite/src/payloads.rs
**PRIORITY: LOW**
**FINDINGS:**

1. **[Line 1-4] PATTERN: Incomplete documentation**
   **CURRENT:** Truncated module documentation
   **SUGGESTION:** Complete the documentation sentence
   **IMPACT:** readability | **EFFORT:** trivial

2. **[Line 13-31] PATTERN: Consistent JSON field naming**
   **CURRENT:** All fields use snake_case (good consistency)
   **SUGGESTION:** No changes needed - follows established patterns
   **IMPACT:** n/a | **EFFORT:** n/a

3. **[Line 48-57] PATTERN: HashMap without capacity hint**
   **CURRENT:** `hints: HashMap<String, JsonValue>`
   **SUGGESTION:** Consider `HashMap::with_capacity(n)` if typical size is known
   **IMPACT:** performance | **EFFORT:** trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-system-satellite/src/dbus_watcher.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 106-112] PATTERN: Repeated error pattern**
   **CURRENT:** Manual error conversion with formatting
   **SUGGESTION:** Create helper function or use established error handling patterns
   **IMPACT:** maintainability | **EFFORT:** small

2. **[Line 44-48] PATTERN: Complex closure parameter**
   **CURRENT:** Long closure parameter list passed to spawn_monitor
   **SUGGESTION:** Create struct to hold monitor configuration parameters
   **IMPACT:** readability | **EFFORT:** small

3. **[Line 78-92] PATTERN: Loop with sleep retry pattern**
   **CURRENT:** Manual retry loop implementation
   **SUGGESTION:** Use tokio-retry crate which is already available in workspace dependencies
   **IMPACT:** maintainability | **EFFORT:** small

---

## SUMMARY RECOMMENDATIONS

**Highest Impact/Lowest Effort (Immediate):**
1. Add `#[must_use]` attributes to important return types
2. Replace TODO/placeholder implementations with proper errors
3. Use `#[derive(Default)]` where appropriate
4. Fix incomplete documentation strings

**High Impact/Small Effort (Next sprint):**
1. Extract complex nested functions for better readability  
2. Use established error handling patterns consistently
3. Replace manual retry loops with tokio-retry
4. Optimize iterator usage patterns

**Medium Impact/Moderate Effort (Future refactoring):**
1. Complete incomplete implementations in unified_processor.rs
2. Implement builder patterns for complex configuration structs
3. Add comprehensive context to custom error types

The codebase shows strong architectural consistency overall. The main opportunities are in completing placeholder implementations and applying established patterns more consistently across all modules.

## Agent 7: Analyze fs and system satellites for refactoring

# Deep Micro-Refactoring Analysis: Desktop Satellite + Health Aggregator

## ARCHITECTURAL CONTEXT:
- **Key patterns observed**: 
  - Project uses `SinexError` for rich error handling with context chains and categorization
  - ULID-based IDs with time-ordering guarantees and PostgreSQL integration via UUID casting
  - `StatefulStreamProcessor` trait provides unified satellite architecture
  - `Event::from_payload()` pattern for structured event creation
  - `sinex_satellite_sdk::processor_main!` macro eliminates main() boilerplate
  
- **Available utilities not being used**:
  - `sinex_macros::with_context` for automatic error context enrichment
  - Project's established sanitization functions in `sinex_core::types`
  - Blob storage patterns already established in `sinex_satellite_sdk::annex`
  - Structured HashMap construction patterns available in the codebase

- **Inconsistencies with project standards**:
  - Manual error handling instead of using established `SinexError` methods
  - Not leveraging project's caching and state management patterns
  - Repetitive configuration parsing that could use helper functions

---

## FILE ANALYSIS

### FILE: crate/satellites/sinex-desktop-satellite/src/lib.rs
**PRIORITY: LOW**
**FINDINGS:**

1. [Line 19] PATTERN: Unnecessary re-export
   CURRENT: `pub use sinex_core::db::models::RawEvent;`
   SUGGESTION: Remove - not used by consuming modules and creates dependency coupling
   IMPACT: maintainability
   EFFORT: trivial

### FILE: crate/satellites/sinex-desktop-satellite/src/main.rs
**PRIORITY: LOW**
**FINDINGS:**

1. [Line 14] PATTERN: Correct macro usage
   CURRENT: `sinex_satellite_sdk::processor_main!(DesktopProcessor);`
   SUGGESTION: No changes needed - follows established pattern
   IMPACT: consistency
   EFFORT: trivial

### FILE: crate/satellites/sinex-desktop-satellite/src/clipboard.rs
**PRIORITY: HIGH**
**FINDINGS:**

[✓ DONE] 1. [Line 87-112] PATTERN: Magic numbers should be constants
   CURRENT: `max_preview_length: 100, max_content_size: 10 * 1024 * 1024`
   SUGGESTION: Extract to constants at module level
   ```rust
   const DEFAULT_MAX_PREVIEW_LENGTH: usize = 100;
   const DEFAULT_MAX_CONTENT_SIZE: usize = 10 * 1024 * 1024; // 10MB
   const DEFAULT_MAX_HISTORY_ENTRIES: usize = 1000;
   ```
   IMPACT: maintainability | readability
   EFFORT: trivial

2. [Line 181-184] PATTERN: String allocation in hot path
   CURRENT: `blake3::hash(content.as_bytes()).to_hex().to_string()`
   SUGGESTION: Use `to_hex()` directly as it returns String
   ```rust
   fn calculate_hash(&self, content: &str) -> String {
       blake3::hash(content.as_bytes()).to_hex()[..16].to_string() // First 16 chars for shorter hashes
   }
   ```
   IMPACT: performance
   EFFORT: trivial

3. [Line 235-251, 260-277] PATTERN: Duplicated path sanitization logic
   CURRENT: Two nearly identical sanitization blocks
   SUGGESTION: Extract to helper method
   ```rust
   fn sanitize_path_component(path_str: &str) -> String {
       let path = std::path::Path::new(path_str);
       if let Some(filename) = path.file_name().and_then(|f| f.to_str()) {
           let sanitized_name = sinex_core::types::sanitize_filename_component(filename)
               .unwrap_or_else(|_| filename.to_string());
           path.parent()
               .map(|parent| parent.join(&sanitized_name).to_string_lossy().to_string())
               .unwrap_or_else(|| sanitized_name)
       } else {
           path_str.to_string()
       }
   }
   ```
   IMPACT: maintainability | readability
   EFFORT: small

4. [Line 561-585, 623-650] PATTERN: Duplicated large content storage logic
   CURRENT: Nearly identical blob storage handling in two methods
   SUGGESTION: Extract to helper method
   ```rust
   async fn handle_large_content(
       &self, 
       content: &ClipboardContent
   ) -> (Option<String>, Option<String>, Option<String>) {
       if content.size_bytes <= self.max_content_size {
           return (content.text_preview.clone(), None, None);
       }
       
       match self.store_large_content(&content.text, &content.hash).await {
           Ok((key, id)) => {
               info!("Stored large content ({} bytes) in blob storage: {}", 
                     content.size_bytes, key);
               (Some("[Content stored in blob storage]".to_string()), Some(key), id)
           }
           Err(e) => {
               error!("Failed to store large content: {}", e);
               (Some("[Content too large - storage failed]".to_string()), None, None)
           }
       }
   }
   ```
   IMPACT: maintainability | readability
   EFFORT: small

5. [Line 671-748] PATTERN: Method doing too much
   CURRENT: `check_clipboard_changes` handles both clipboard and primary selection
   SUGGESTION: Split into separate methods
   ```rust
   async fn check_clipboard_changes(&mut self, tx: &mpsc::UnboundedSender<RawEvent>) -> SatelliteResult<()> {
       self.check_main_clipboard(tx).await?;
       if self.enable_primary_selection {
           self.check_primary_selection(tx).await?;
       }
       Ok(())
   }
   
   async fn check_main_clipboard(&mut self, tx: &mpsc::UnboundedSender<RawEvent>) -> SatelliteResult<()> {
       // Main clipboard logic
   }
   
   async fn check_primary_selection(&mut self, tx: &mpsc::UnboundedSender<RawEvent>) -> SatelliteResult<()> {
       // Primary selection logic  
   }
   ```
   IMPACT: readability | maintainability
   EFFORT: moderate

6. [Line 695, 731] PATTERN: Repetitive channel error handling
   CURRENT: `if tx.send(event).is_err() { warn!("Event channel closed"); return Ok(()); }`
   SUGGESTION: Extract to helper method or use ? operator pattern
   ```rust
   fn send_event_or_warn(&self, tx: &mpsc::UnboundedSender<RawEvent>, event: RawEvent) -> bool {
       if tx.send(event).is_err() {
           warn!("Event channel closed");
           false
       } else {
           true
       }
   }
   ```
   IMPACT: maintainability
   EFFORT: small

### FILE: crate/satellites/sinex-desktop-satellite/src/unified_processor.rs  
**PRIORITY: HIGH**
**FINDINGS:**

1. [Line 303-337] PATTERN: Repetitive JSON deserialization
   CURRENT: Repeated `serde_json::from_value` calls with manual error handling
   SUGGESTION: Create helper method
   ```rust
   fn parse_config_value<T: serde::de::DeserializeOwned>(&self, key: &str, ctx: &StreamProcessorContext) -> Option<T> {
       ctx.config.get(key)
           .and_then(|json| serde_json::from_value::<T>(json.clone()).ok())
   }
   
   // Usage:
   if let Some(enabled) = self.parse_config_value::<bool>("clipboard_enabled", &ctx) {
       self.config.clipboard_enabled = enabled;
   }
   ```
   IMPACT: maintainability | readability  
   EFFORT: small

2. [Line 455-473] PATTERN: Verbose HashMap construction
   CURRENT: Manual HashMap::from calls
   SUGGESTION: Use helper macro or collect pattern
   ```rust
   let processor_stats: HashMap<String, u64> = [
       ("clipboard_enabled", if self.config.clipboard_enabled { 1 } else { 0 }),
       ("window_manager_enabled", if self.config.window_manager_enabled { 1 } else { 0 }),
       ("successful_targets", successful_targets.len() as u64),
       ("failed_targets", failed_targets.len() as u64),
   ].into_iter()
    .map(|(k, v)| (k.to_string(), v))
    .collect();
   ```
   IMPACT: readability
   EFFORT: trivial

[✓ DONE] 3. [Line 536, 537] PATTERN: Magic numbers in calculations
   CURRENT: `Duration::from_millis(adjusted_events * 10), adjusted_events * 256`
   SUGGESTION: Extract to constants
   ```rust
   const MS_PER_EVENT: u64 = 10;
   const BYTES_PER_EVENT: u64 = 256;
   
   estimated_duration: Duration::from_millis(adjusted_events * MS_PER_EVENT),
   estimated_data_size: adjusted_events * BYTES_PER_EVENT,
   ```
   IMPACT: maintainability
   EFFORT: trivial

4. [Line 582-594] PATTERN: Repetitive serde_json::to_value calls
   CURRENT: Multiple separate `serde_json::to_value` calls
   SUGGESTION: Use helper or collect pattern
   ```rust
   let metadata = [
       ("clipboard_enabled", &self.config.clipboard_enabled),
       ("window_manager_enabled", &self.config.window_manager_enabled),
       ("window_manager_type", &self.config.window_manager_type),
       ("clipboard_poll_interval_secs", &self.config.clipboard_poll_interval_secs),
   ].into_iter()
    .map(|(k, v)| (k.to_string(), serde_json::to_value(v).unwrap()))
    .collect();
   ```
   IMPACT: readability | maintainability
   EFFORT: small

### FILE: crate/satellites/sinex-desktop-satellite/src/window_manager.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. [Line 154, 161-165, 200-205] PATTERN: Unused fields with underscore prefix
   CURRENT: Multiple `_` prefixed fields that are stored but never used
   SUGGESTION: Either implement the functionality or remove dead fields
   ```rust
   // Remove unused fields entirely if not implementing functionality:
   struct WindowManagerWatcher {
       // Remove: _focus_history, _window_augmentation, etc.
       // Keep only actively used fields
   }
   ```
   IMPACT: maintainability | clarity
   EFFORT: small

[✓ DONE] 2. [Line 254, 328] PATTERN: Magic numbers for cache timeouts  
   CURRENT: `Duration::from_secs(60), Duration::from_secs(30)`
   SUGGESTION: Extract to constants
   ```rust
   const CACHE_CLEANUP_INTERVAL_SECS: u64 = 60;
   const CACHE_ENTRY_MAX_AGE_SECS: u64 = 30;
   ```
   IMPACT: maintainability
   EFFORT: trivial

3. [Line 538, 582, 625, 653, 690, 718] PATTERN: Repeated channel error handling
   CURRENT: Identical error handling pattern repeated 6 times
   SUGGESTION: Extract to helper method
   ```rust
   fn send_event_or_warn(tx: &mpsc::UnboundedSender<RawEvent>, event: RawEvent) -> bool {
       if tx.send(event).is_err() {
           warn!("Event channel closed");
           false
       } else {
           true
       }
   }
   ```
   IMPACT: maintainability
   EFFORT: small

4. [Line 748-762] PATTERN: Repetitive serde_json::to_value mapping
   CURRENT: Three identical `.map(|w| serde_json::to_value(w).unwrap()).collect()` patterns
   SUGGESTION: Extract helper function
   ```rust
   fn serialize_values<T: serde::Serialize>(values: impl Iterator<Item = T>) -> Vec<serde_json::Value> {
       values.map(|v| serde_json::to_value(v).unwrap()).collect()
   }
   
   // Usage:
   windows: serialize_values(self.windows.values()),
   workspaces: serialize_values(self.workspaces.values()),  
   monitors: serialize_values(self.monitors.values()),
   ```
   IMPACT: readability | maintainability
   EFFORT: small

5. [Line 569, 682, 710] PATTERN: Hardcoded parsing with unwrap_or(0)
   CURRENT: `workspace_id.parse().unwrap_or(0)`
   SUGGESTION: Extract to helper with better error handling
   ```rust
   fn parse_id(id_str: &str, context: &str) -> i32 {
       id_str.parse().unwrap_or_else(|_| {
           warn!("Failed to parse {} '{}', defaulting to 0", context, id_str);
           0
       })
   }
   ```
   IMPACT: maintainability | debugging
   EFFORT: trivial

### FILE: crate/satellites/sinex-health-aggregator/src/lib.rs
**PRIORITY: LOW**
**FINDINGS:**

1. [Line 74-83] PATTERN: Repetitive ScanReport construction
   CURRENT: Nearly identical ScanReport creation
   SUGGESTION: Extract to helper method
   ```rust
   fn create_empty_scan_report(start_time: DateTime<Utc>) -> ScanReport {
       ScanReport {
           events_processed: 0,
           duration: std::time::Duration::from_secs(0),
           final_checkpoint: Checkpoint::None,
           time_range: Some((start_time, Utc::now())),
           processor_stats: HashMap::new(),
           successful_targets: vec!["health".to_string()],
           failed_targets: Vec::new(),
           warnings: Vec::new(),
       }
   }
   ```
   IMPACT: maintainability
   EFFORT: trivial

### FILE: crate/satellites/sinex-health-aggregator/src/unified_processor.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. [Line 146] PATTERN: Missing import
   CURRENT: `Event::from_payload()` used but `Event` not imported
   SUGGESTION: Add import
   ```rust
   use sinex_core::types::events::Event;
   ```
   IMPACT: correctness
   EFFORT: trivial

2. [Line 37, 45, 120, 155] PATTERN: Missing struct field trailing commas
   CURRENT: Inconsistent comma usage in struct literals
   SUGGESTION: Add trailing commas for consistency
   ```rust
   pub struct SystemHealthSummary {
       // ... fields ...
       pub components: HashMap<String, ComponentHealth>, // Add comma
   }
   ```
   IMPACT: consistency
   EFFORT: trivial

3. [Line 259, 313] PATTERN: Undefined method calls
   CURRENT: `self.process_batch()` and `self.get_checkpoint_data()` called but not defined
   SUGGESTION: Either implement methods or remove calls
   ```rust
   // Either add these methods or replace with appropriate implementation
   async fn process_batch(&mut self, events: Vec<RawEvent>) -> SatelliteResult<BatchProcessingResult> {
       // Implementation needed
   }
   
   async fn get_checkpoint_data(&self) -> Option<serde_json::Value> {
       // Implementation needed  
   }
   ```
   IMPACT: correctness
   EFFORT: moderate

4. [Line 126, 128, 131] PATTERN: Magic numbers for health thresholds
   CURRENT: `Duration::minutes(2), Duration::minutes(5)` hardcoded
   SUGGESTION: Extract to constants
   ```rust
   const HEALTHY_THRESHOLD_MINUTES: i64 = 2;
   const DEGRADED_THRESHOLD_MINUTES: i64 = 5;
   
   if age < Duration::minutes(HEALTHY_THRESHOLD_MINUTES) {
       summary.healthy_components += 1;
   } else if age < Duration::minutes(DEGRADED_THRESHOLD_MINUTES) {
       summary.degraded_components += 1;
   }
   ```
   IMPACT: maintainability
   EFFORT: trivial

5. [Line 210-218, 268-276, 289-297] PATTERN: Repetitive ScanReport construction
   CURRENT: Three nearly identical ScanReport constructions
   SUGGESTION: Extract builder or helper method
   ```rust
   fn build_scan_report(
       events_processed: u64,
       start_time: DateTime<Utc>,
       checkpoint: Checkpoint,
       time_range: Option<(DateTime<Utc>, DateTime<Utc>)>,
       target: &str
   ) -> ScanReport {
       ScanReport {
           events_processed,
           duration: std::time::Duration::from_secs(0), // Or calculate properly
           final_checkpoint: checkpoint,
           time_range,
           processor_stats: HashMap::new(),
           successful_targets: vec![target.to_string()],
           failed_targets: HashMap::new(),
           warnings: Vec::new(),
       }
   }
   ```
   IMPACT: maintainability | readability
   EFFORT: small

**SUMMARY RECOMMENDATIONS:**
1. **Highest Impact**: Fix missing imports and undefined method calls in health aggregator
2. **Quick Wins**: Extract magic numbers to constants across all files  
3. **Code Quality**: Consolidate repetitive error handling and data structure construction patterns
4. **Maintainability**: Break down overly long methods and extract duplicated logic to helpers

## Agent 8: Analyze desktop and health satellites for refactoring

Based on my analysis of the codebase, I can now provide a comprehensive micro-refactoring analysis for the automaton services.

## ARCHITECTURAL CONTEXT:

**Key patterns observed:**
- **Unified StatefulStreamProcessor Architecture**: All automatons implement the `StatefulStreamProcessor` trait from `sinex-satellite-sdk`
- **Rich Error Handling**: Project uses `SinexError` with context chains and categorization 
- **ULID-based IDs**: Time-ordered, distributed-safe primary keys throughout
- **Event-Driven Design**: Immutable events in `core.events` with provenance tracking
- **Satellite SDK Integration**: Standard lifecycle management, checkpointing, gRPC communication
- **Services Layer**: High-level business logic in `sinex-services` crate

**Available utilities not being used:**
- `SinexError` builder methods (`with_context`, `with_operation`, `with_path`, etc.)
- `ResultExt` trait for error context enrichment
- sinex-core prelude with common types
- Extensive derive macros from `sinex-macros`
- `processor_main!` macro for standardized CLI

**Inconsistencies with project standards:**
- Mixture of `lib.rs` and `unified_processor.rs` implementations (duplication)
- Inconsistent error handling patterns (some using `eyre!`, others using `SatelliteError::Processing`)
- Missing use of project's rich error context features
- Some implementations not leveraging the services layer properly

## DETAILED ANALYSIS:

### FILE: /realm/project/sinex/crate/satellites/sinex-analytics-automaton/src/lib.rs
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Line 46-57] PATTERN: Inconsistent StatefulStreamProcessor Interface**
   CURRENT: Using deprecated `initialize` signature with `Config` associated type
   SUGGESTION: Update to match current trait signature without Config type parameter
   IMPACT: consistency|correctness  
   EFFORT: small

2. **[Line 68-84] PATTERN: Stubbed Implementation Pattern**
   CURRENT: `match until { TimeHorizon::Snapshot => 0, ... }` with hardcoded zeros
   SUGGESTION: Add TODO comments or implement basic analytics operations using `sinex-services::AnalyticsService`
   IMPACT: maintainability|functionality
   EFFORT: moderate

3. **[Line 105-148] PATTERN: Code Duplication**
   CURRENT: Nearly identical `ExplorationProvider` implementation across all automata
   SUGGESTION: Extract to shared trait or macro in `sinex-satellite-sdk`
   IMPACT: maintainability|consistency
   EFFORT: moderate

### FILE: /realm/project/sinex/crate/satellites/sinex-analytics-automaton/src/unified_processor.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 38-43] PATTERN: Inconsistent Interface**
   CURRENT: `initialize` method signature doesn't match the lib.rs version
   SUGGESTION: Reconcile interface differences or remove duplicate file
   IMPACT: consistency|maintainability
   EFFORT: small

2. **[Line 92-98] PATTERN: Missing Required Fields**
   CURRENT: `SourceState` creation missing required fields like `healthy` and `recent_activity`
   SUGGESTION: Use builder pattern or add missing fields with appropriate defaults
   IMPACT: correctness|functionality
   EFFORT: trivial

3. **[Line 113-122] PATTERN: Inconsistent Field Names**
   CURRENT: `missing_items` field in `CoverageAnalysis` 
   SUGGESTION: Match the interface from lib.rs using `missing_samples` and `missing_count`
   IMPACT: consistency|correctness
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-content-automaton/src/lib.rs  
**PRIORITY: HIGH**
**FINDINGS:**

1. **[Line 46-57] PATTERN: Identical Implementation to Analytics**
   CURRENT: Copy-paste implementation with only processor name changed
   SUGGESTION: Extract common base implementation or use macro generation
   IMPACT: maintainability|consistency
   EFFORT: moderate

2. **[Line 105-148] PATTERN: Hardcoded Processor Description**
   CURRENT: `"Content processor"` static string
   SUGGESTION: Use configurable description or derive from processor type
   IMPACT: maintainability|consistency
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/satellites/sinex-content-automaton/src/unified_processor.rs
**PRIORITY: LOW**
**FINDINGS:**

1. **[Line 25-27] PATTERN: Ad-hoc Error Construction**
   CURRENT: `fn missing_field(field: &str) -> SatelliteError { SatelliteError::General(eyre!("Missing {}", field)) }`
   SUGGESTION: Use `SinexError::validation()` with context: `SinexError::validation("Missing required field").with_context("field", field)`
   IMPACT: consistency|readability
   EFFORT: trivial

2. **[Line 42-50] PATTERN: Nested Option/Result Handling**
   CURRENT: `request.get("method").and_then(|v| v.as_str()).unwrap_or("")`
   SUGGESTION: Use `?` operator with proper error context or Result combinators
   IMPACT: readability|error handling
   EFFORT: small

### FILE: /realm/project/sinex/crate/satellites/sinex-pkm-automaton/src/lib.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 107] PATTERN: Redundant Import**
   CURRENT: `use std::collections::HashMap;` inside function scope
   SUGGESTION: Move to module level or remove if using workspace HashMap
   IMPACT: consistency|readability
   EFFORT: trivial

2. **[Line 108-116] PATTERN: Identical SourceState Construction**
   CURRENT: Same pattern as other automata with only description changed
   SUGGESTION: Create shared factory function or use macro
   IMPACT: maintainability|consistency
   EFFORT: small

### FILE: /realm/project/sinex/crate/satellites/sinex-search-automaton/src/lib.rs
**PRIORITY: MEDIUM**
**FINDINGS:**

1. **[Line 105-148] PATTERN: Complete Code Duplication**
   CURRENT: Exact same `ExplorationProvider` implementation as other automata
   SUGGESTION: Extract to shared implementation in SDK
   IMPACT: maintainability|consistency
   EFFORT: moderate

### Cross-cutting Issues:

**PATTERN: Main Entry Point Inconsistency**
**FILES: All main.rs files**
CURRENT: All using `processor_main!` macro correctly
SUGGESTION: No changes needed - this is the correct pattern
IMPACT: consistency
EFFORT: none

**PATTERN: Missing Service Layer Integration**
**FILES: Most lib.rs implementations**  
CURRENT: Stub implementations not using `sinex-services`
SUGGESTION: Integrate with `AnalyticsService`, `ContentService`, `PkmService`, `SearchService` respectively
IMPACT: functionality|architecture
EFFORT: moderate

**PATTERN: Inconsistent Error Handling**
**FILES: unified_processor.rs files**
CURRENT: Mix of `eyre!`, `SatelliteError::General`, manual error construction
SUGGESTION: Standardize on `SinexError` with context patterns from the project
IMPACT: consistency|maintainability
EFFORT: small

**PRIORITY SUMMARY:**
- **HIGH**: Interface inconsistencies, major code duplication
- **MEDIUM**: Missing functionality, architectural patterns not followed
- **LOW**: Style inconsistencies, minor optimizations

The main finding is extensive code duplication across automata with only cosmetic differences (processor names). This suggests the need for better abstractions in the satellite SDK or code generation approaches.

## Agent 9: Analyze automaton services for refactoring

Based on my comprehensive analysis, I can now provide the systematic micro-refactoring analysis:

## ARCHITECTURAL CONTEXT:
- **Key patterns observed**: Project uses comprehensive custom error handling (SinexError), ULID primary keys, structured event system, StatefulStreamProcessor trait for consistency, stage-as-you-go pattern for provenance, extensive use of tracing, workspace-wide dependency management
- **Available utilities not being used**: Project has rich error handling utilities, comprehensive test framework (sinex-test-utils), database query helpers, and common types that could reduce boilerplate
- **Inconsistencies with project standards**: Some files lack the comprehensive error context that SinexError provides, manual trait implementations that could use derives, inconsistent documentation patterns, missing tracing instrumentation in some areas

## FILE ANALYSIS

### FILE: /realm/project/sinex/crate/satellites/sinex-document-ingestor/src/lib.rs
PRIORITY: MEDIUM
FINDINGS:

1. [Line 70-73] PATTERN: Error handling inconsistency
   CURRENT: Manual error creation without using project's error patterns
   SUGGESTION: Use SinexError::processing() with proper context chaining
   IMPACT: consistency|readability
   EFFORT: trivial

2. [Line 87-90] PATTERN: Missing tracing instrumentation
   CURRENT: mime_guess operation without logging
   SUGGESTION: Add debug! log for mime type determination
   IMPACT: maintainability
   EFFORT: trivial

3. [Line 213-218] PATTERN: Manual path conversion with verbose error handling
   CURRENT: camino::Utf8PathBuf::from_path_buf(entry_path).map_err(...)
   SUGGESTION: Extract to helper function or use .with_context() from project's ResultExt
   IMPACT: readability|consistency
   EFFORT: small

4. [Line 278-300] PATTERN: Default implementation not following project patterns
   CURRENT: Manual capabilities definition
   SUGGESTION: Consider using builder pattern or derive macro if available
   IMPACT: consistency
   EFFORT: small

5. [Line 155-167] PATTERN: String magic values for material types
   CURRENT: Hardcoded strings like "document.text"
   SUGGESTION: Define as named constants or enum variants
   IMPACT: maintainability
   EFFORT: small

### FILE: /realm/project/sinex/crate/satellites/sinex-document-ingestor/src/main.rs
PRIORITY: LOW
FINDINGS:

1. [Line 11-12] PATTERN: Consistent with project macro pattern
   CURRENT: sinex_satellite_sdk::processor_main!(DocumentProcessor);
   SUGGESTION: No changes needed - follows project conventions perfectly
   IMPACT: none
   EFFORT: none

### FILE: /realm/project/sinex/crate/core/sinex-rpc-dispatcher/src/lib.rs
PRIORITY: HIGH
FINDINGS:

1. [Line 17-30] PATTERN: Underutilized configuration structure
   CURRENT: Generic HashMap<String, serde_json::Value> for server_config
   SUGGESTION: Define typed configuration struct for server settings
   IMPACT: type_safety|maintainability
   EFFORT: small

2. [Line 57-82] PATTERN: No-op scan implementation
   CURRENT: All scan modes return 0 events_processed with no actual logic
   SUGGESTION: Add TODO comments or warning logs about unimplemented functionality
   IMPACT: maintainability|correctness
   EFFORT: trivial

3. [Line 64-70] PATTERN: Repetitive match arms
   CURRENT: match until { Snapshot => 0, Historical => 0, Continuous => 0 }
   SUGGESTION: Simplify to let events_processed = 0; // All modes unimplemented
   IMPACT: readability
   EFFORT: trivial

4. [Line 103-146] PATTERN: Placeholder implementation returning empty values
   CURRENT: All ExplorationProvider methods return empty/default values
   SUGGESTION: Either implement properly or return NotImplemented errors using SinexError
   IMPACT: correctness|maintainability
   EFFORT: moderate

5. [Line 125-127] PATTERN: Hardcoded time range
   CURRENT: time_range: (now - chrono::Duration::days(1), now)
   SUGGESTION: Use configuration or make it a parameter
   IMPACT: maintainability
   EFFORT: small

### FILE: /realm/project/sinex/crate/core/sinex-rpc-dispatcher/src/main.rs
PRIORITY: LOW
FINDINGS:

1. [Line 11-12] PATTERN: Consistent with project macro pattern
   CURRENT: sinex_satellite_sdk::processor_main!(RpcDispatcherProcessor);
   SUGGESTION: No changes needed - follows project conventions
   IMPACT: none
   EFFORT: none

### FILE: /realm/project/sinex/crate/core/sinex-sensd/src/config.rs
PRIORITY: LOW
FINDINGS:

1. [Line 124-146] PATTERN: Environment variable parsing without error context
   CURRENT: std::env::var("SENSD_GRPC_PORT").ok().and_then(|s| s.parse().ok())
   SUGGESTION: Use project's error handling with context for parse failures
   IMPACT: maintainability|debugging
   EFFORT: small

2. [Line 82] PATTERN: Hardcoded default database path
   CURRENT: "postgresql:///sinex_dev?host=/run/postgresql"
   SUGGESTION: Consider making it more portable or add comment about NixOS assumption
   IMPACT: maintainability
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/core/sinex-sensd/src/lib.rs
PRIORITY: LOW
FINDINGS:

1. [Line 1-15] PATTERN: Standard module layout
   CURRENT: Clean module declarations and re-exports
   SUGGESTION: No changes needed - follows Rust conventions
   IMPACT: none
   EFFORT: none

### FILE: /realm/project/sinex/crate/core/sinex-sensd/src/main.rs
PRIORITY: MEDIUM
FINDINGS:

1. [Line 18-21] PATTERN: Complex tracing initialization
   CURRENT: Manual registry().with().with().init() chain
   SUGGESTION: Extract to helper function or use project's tracing utilities
   IMPACT: maintainability|consistency
   EFFORT: small

2. [Line 33-38] PATTERN: Basic signal handling
   CURRENT: Simple CTRL+C handler
   SUGGESTION: Consider using project's graceful shutdown patterns if available
   IMPACT: consistency
   EFFORT: small

3. [Line 41-51] PATTERN: Verbose tokio::select! with repeated patterns
   CURRENT: Separate error handling for service vs shutdown
   SUGGESTION: Could be simplified with proper error context
   IMPACT: readability
   EFFORT: small

### FILE: /realm/project/sinex/crate/core/sinex-gateway/src/cascade_analyzer.rs
PRIORITY: LOW
FINDINGS:

1. [Line 100-101] PATTERN: String formatting for temp table names
   CURRENT: format!("cascade_analysis_{}", Ulid::new())
   SUGGESTION: Consider using a const prefix and ensure temp table cleanup on failure
   IMPACT: maintainability
   EFFORT: small

2. [Line 167-190] PATTERN: Manual UUID conversion in batch
   CURRENT: event_ids.iter().map(|id| ulid_to_uuid(*id)).collect()
   SUGGESTION: Extract to helper function since this pattern appears multiple times
   IMPACT: readability|consistency
   EFFORT: trivial

### FILE: /realm/project/sinex/crate/core/sinex-gateway/src/replay_state_machine.rs
PRIORITY: MEDIUM
FINDINGS:

1. [Line 44-92] PATTERN: Large match expression for state transitions
   CURRENT: Manual state transition validation with repetitive patterns
   SUGGESTION: Consider state transition table or macro to reduce duplication
   IMPACT: maintainability
   EFFORT: moderate

2. [Line 138-169] PATTERN: Large struct with many Option fields
   CURRENT: ReplayOperation with 15+ fields, many optional
   SUGGESTION: Consider using builder pattern with bon derive already available in workspace
   IMPACT: usability|maintainability
   EFFORT: moderate

3. [Line 124-135] PATTERN: Manual Default implementation
   CURRENT: Explicit Default impl for ReplayCheckpoint
   SUGGESTION: Use #[derive(Default)] since all fields have reasonable defaults
   IMPACT: maintainability
   EFFORT: trivial

## SUMMARY

**Highest Priority Issues:**
1. RPC Dispatcher placeholder implementations should either be implemented or return proper NotImplemented errors
2. Inconsistent error handling patterns across document ingestor
3. Large state machine structs could benefit from builder patterns

**Most Common Patterns:**
1. Missing use of workspace's rich error handling utilities
2. Manual implementations that could use derives or macros
3. Hardcoded values that should be constants or configuration

**Quick Wins:**
1. Add #[derive(Default)] where appropriate
2. Extract common UUID conversion patterns to helpers
3. Add tracing instrumentation to key operations
4. Define string constants for magic values

The codebase shows good architectural consistency but has opportunities for leveraging more of the project's established utilities and patterns for better maintainability and consistency.

## Agent 10: Analyze document ingestor and remaining code for refactoring

---

# COMPREHENSIVE MICRO-REFACTORING PLAN

Based on the analysis from all 10 agents, here is the complete action plan with ALL specific line numbers and details:

## IMMEDIATE ACTIONS - Obsolete File Removal

### Files to DELETE completely:
[✓ DONE] - `/realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/main_old.rs`
[✓ DONE] - `/realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/main_old_nats.rs`
[✓ DONE] - `/realm/project/sinex/crate/satellites/sinex-terminal-satellite/src/main_unified.rs`

## SECTION 1: sinex-services Layer Refactoring

### error.rs - Replace Custom Error with SinexError
- **Lines 8-33**: Delete entire ServiceError enum
- **Lines 11-14**: Remove manual From implementations
- **Lines 17-26**: Replace with SinexError builder methods

### analytics.rs
- **Lines 26-48**: Extract time-range query logic to `fn apply_time_filters()`
- **Line 41**: Define `const EPOCH_START: DateTime<Utc> = DateTime::from_timestamp(0, 0).unwrap();`
- **Lines 92-96, 144-148**: Create `fn minutes_to_interval(minutes: i32) -> PgInterval`
- **Lines 131-135**: Replace with `commands.into_iter().map(|c| (c.command, c.count)).collect()`

### content.rs
- **Line 9**: Remove `_pool: DbPool` field
- **Lines 42-51**: Delete duplicate `store_content` method
- **Lines 34-35, 58-59, 72-74**: Use `SinexError::processing().with_operation("blob_storage")`

### search.rs
- **Lines 41-111**: Replace with SeaQuery builder pattern
- **Line 62**: Use `query.bind(&query.sources[..])` 
- **Lines 99-111**: Create SearchResult struct with FromRow derive
- **Line 88**: Replace ILIKE with PostgreSQL full-text search
- **Lines 141-145**: Fix with `payload_str.char_indices().nth(150).map_or(payload_str, |(i, _)| &payload_str[..i])`

### pkm.rs
- **Lines 78-87**: Create static HashMap for entity mapping
- **Lines 35, 89-93**: Extract `fn build_metadata() -> serde_json::Value`
- **Lines 253-261**: Extract `fn calculate_checksums(content: &[u8]) -> (String, String)`
- **Lines 327-339, 354-364**: Create MaterialSummary struct
- **Lines 182-186**: Extract `fn safe_content_preview(content: &str) -> String`

## SECTION 2: SDK/Migrations/Macros Refactoring

### sinex-satellite-sdk/src/config.rs
- **Lines 259-275**: Extract `fn env_var_or_default(key: &str, default: fn() -> String) -> String`
- **Lines 354-358**: Extract `fn get_cache_dir() -> Utf8PathBuf`
- **Lines 390-394**: Add `.with_context("path", path)` to error

### sinex-satellite-sdk/src/grpc_client.rs
- **Lines 21-22**: Use single string variable
- **Lines 114-118**: Check protobuf &str support
- **Lines 47-52**: Add `.with_context("event_id", event_id)`
- [✓ DONE] **Line 164**: Add `#[inline]`

### sinex-satellite-sdk/src/stream_processor.rs
- **Lines 232-237**: Add `#[derive(bon::Builder)]` to Checkpoint
- **Lines 674-675**: Add `#[allow(unused_variables)]`
- **Line 439**: Fix macro or add issue reference
- **Lines 1048-1051**: Return `Err(SatelliteError::Configuration("..."))`

### sinex-satellite-sdk/src/cli.rs
- **Lines 138-150**: Extract `fn parse_checkpoint_json()`, `fn parse_checkpoint_timestamp()`, `fn parse_checkpoint_stream()`
- **Line 138**: Use `checkpoint_str.eq_ignore_ascii_case("none")`

### sinex-macros/src/error_context.rs
- **Line 46**: Split into `validate_attributes()`, `build_context()`, `generate_code()`
- **Lines 248-253**: Use proc_macro_error crate
- **Lines 339-340**: Remove unused variable

### sinex-test-utils/src/lib.rs
- **Line 721**: Implement `impl From<TestError> for SinexError`
- **Line 790**: Use `assert_matches!` macro

### sinex-test-utils/src/builders.rs
- **Lines 66-77**: Add `#[derive(bon::Builder)]`

### sinex-migrations/src/lib.rs
- **Lines 28-49**: Create `macro_rules! migration_vec`

## SECTION 3: sinex-ingestd Refactoring

### lib.rs
- **Lines 26-57**: Replace IngestdError with SinexError

### main.rs
- **Lines 82-94**: Extract `fn validate_config(config: &Config) -> Result<()>`

### config.rs
- **Lines 81-103**: Use bon::Builder
- **Lines 232-237**: Use `url::Url::parse()`

### service.rs
- **Lines 39-40**: Use `const VERSION: &str = env!("CARGO_PKG_VERSION");`
- **Lines 628-686**: Extract `fn build_batch_insert_query()`
- **Lines 296-347**: Extract `fn setup_shutdown_handler()`
- **Lines 520-523**: Use telemetry macros
- **Line 43**: Create `struct SubjectCache(Arc<Mutex<HashMap<...>>>)`
- **Lines 671-677**: Use actual schema values

### validator.rs
- **Lines 37-42**: Create `struct SchemaCache(Arc<RwLock<...>>)`
- **Lines 25-32**: Use string interning

### figment_config.rs
- **Lines 83-113**: Use `#[serde(default = "default_value")]`
- **Lines 136-163**: Extract `fn setup_figment()`
- **Lines 198-224**: Use validator built-ins

### schema_sync.rs
- **Lines 41-55**: Use `.filter(|r| r.is_ok()).count()`
- **Lines 46-48**: Pre-compute hashes

## SECTION 4: sinex-gateway Refactoring

### main.rs
- **Lines 56-64**: Extract `fn setup_tracing()`
- **Lines 78-79**: Add `.with_operation("start_rpc_server")`

### service_container.rs
- **Lines 28-30**: Use `SinexError::configuration()`
- **Lines 33-35**: Use `db_error()` helper
- **Lines 43-44**: Use `SinexError::io().with_path()`
[✓ DONE] - **Lines 96-98**: Define `const TELEMETRY_INTERVAL_SECS: u64 = 300;`

### handlers.rs
- **Lines 46-52**: Extract `fn parse_ulid_param()`
- **Line 198**: Use proper encoding detection
- **Lines 15-29**: Create `fn extract_param<T>(params: &Value, key: &str) -> Result<T>`

### rpc_server.rs
- **Lines 154-156**: Fix Unix socket vs TCP inconsistency
- **Lines 174-175**: Make address configurable
- **Lines 84-126**: Create dispatch table HashMap

### native_messaging.rs
[✓ DONE] - **Lines 66-69**: Define `const MAX_MESSAGE_SIZE: usize = 1024 * 1024;`
- **Lines 132-161**: Extract shared dispatch logic
- **Line 3**: Use `u32::to_ne_bytes()`

### cascade_analyzer.rs
- **Lines 166-169**: Use `event_ids.to_uuid_vec()`
- **Lines 316-318**: Fix type annotation
- **Line 99**: Use timestamp for temp tables
- **Lines 240-245**: Add `db_error()` context

### replay_state_machine.rs
- **Lines 45-92**: Create state transition table
- **Lines 497-501**: Extract `fn ulid_to_lock_id()`
- **Lines 208-223**: Use `db_error()` helper

## SECTION 5: Terminal Satellites Refactoring

### shell_detection.rs
- **Lines 44, 59**: Extract `fn get_home_dir() -> Option<Utf8PathBuf>`
- **Line 179**: Add command cache HashMap
- **Lines 192-200**: Extract with ? operator
- **Lines 207-217**: Use sysinfo crate

### kitty.rs
- **Lines 128-135**: Use `.map()` and `.collect()`
- **Lines 157-200**: Create `fn io_to_satellite_error(e: io::Error) -> SatelliteError`
- **Lines 202-216**: Extract `fn extract_json_from_framed_response()`
- **Lines 229-284**: Define KittyResponse struct
- **Lines 417-434**: Use `lines().rev().find_map()`
- **Lines 565-581**: Extract duration check

### unified_processor.rs (terminal)
- **Lines 54-82**: Use bon::Builder
- **Lines 204-233**: Extract `fn get_file_metadata()`
- **Lines 237-253**: Define estimation constants
- **Lines 514-577**: Create `fn parse_config_field<T>()`
- **Lines 773-787**: Extract `fn estimate_file_entries()`

### unified_processor.rs (canonicalizer)
- **Lines 40-56**: Use serde_json methods
- **Lines 263-286**: Define EventSource enum
- **Lines 146-150**: Add proper validation

### atuin.rs
- [✓ DONE] **Lines 31-38**: Use `dirs::home_dir()`

### history.rs
- **Lines 70-80**: Use BufReader for large files

## SECTION 6: FS-Watcher/System Satellites Refactoring

### fs-watcher/unified_processor.rs
- [✓ DONE] **Line 278-283**: Add `#[must_use]`
- **Line 429**: Extract `let should_process = self.matches_patterns(utf8_path);`
- **Lines 452-481**: Split into `matches_watch_patterns()`, `matches_ignore_patterns()`
- **Lines 585-590**: Define error constants
- **Lines 613-621**: Pass parameters instead of cloning
- **Lines 787-791**: Implement or add TODO with issue #
- **Lines 870-881**: Extract `configure_watch_patterns()`
- **Line 1056**: Use `.as_str()` instead of `.to_string()` (note: requires type change)

### fs-watcher/cli.rs
- **Line 61**: Return `Err(eyre!("Direct mode not supported"))`
- **Lines 50-89**: Extract `run_direct_mode()`, `run_sensd_mode()`
- **Lines 72-80**: Implement `From<Args> for SensdIntegrationConfig`

### system-satellite/lib.rs
- [✓ DONE] **Lines 35-70**: Add `#[derive(Default)]`
- **Lines 74-87**: Add context methods
- **Lines 89-93**: Use `#[from]` attribute

### system-satellite/unified_processor.rs
- **Lines 112-125**: Use `..Default::default()`
- **Lines 142-147**: Use builder pattern
- **Line 150+**: Complete implementation

### system-satellite/payloads.rs
- [✓ DONE] **Lines 1-4**: Complete documentation
- **Lines 48-57**: Use `HashMap::with_capacity()`

### system-satellite/dbus_watcher.rs
- **Lines 106-112**: Create error helper
- **Lines 44-48**: Create MonitorConfig struct
- **Lines 78-92**: Use tokio-retry

## SECTION 7: Desktop/Health Satellites Refactoring

### desktop-satellite/lib.rs
- [✓ DONE] **Line 19**: Remove unused re-export

### desktop-satellite/clipboard.rs
[✓ DONE] - **Lines 87-112**: Define constants:
  ```rust
  const DEFAULT_MAX_PREVIEW_LENGTH: usize = 100;
  const DEFAULT_MAX_CONTENT_SIZE: usize = 10 * 1024 * 1024;
  const DEFAULT_MAX_HISTORY_ENTRIES: usize = 1000;
  ```
- [✓ DONE] **Lines 181-184**: Remove redundant `.to_string()`
- **Lines 235-251, 260-277**: Extract `fn sanitize_path_component()`
- **Lines 561-585, 623-650**: Extract `fn handle_large_content()`
- **Lines 671-748**: Split into `check_main_clipboard()`, `check_primary_selection()`
- **Lines 695, 731**: Extract `fn send_event_or_warn()`

### desktop-satellite/unified_processor.rs
- **Lines 303-337**: Create `fn parse_config_value<T>()`
- **Lines 455-473**: Use collect pattern
[✓ DONE] - **Lines 536, 537**: Define `const MS_PER_EVENT: u64 = 10;` and `const BYTES_PER_EVENT: u64 = 256;`
- **Lines 582-594**: Use collect pattern

### desktop-satellite/window_manager.rs
- **Lines 154, 161-165, 200-205**: Remove unused fields
[✓ DONE] - **Lines 254, 328**: Define cache constants
- **Lines 538, 582, 625, 653, 690, 718**: Extract `fn send_event_or_warn()`
- **Lines 748-762**: Create `fn serialize_values<T>()`
- **Lines 569, 682, 710**: Create `fn parse_id()`

### health-aggregator/lib.rs
- **Lines 74-83**: Extract `fn create_empty_scan_report()`

### health-aggregator/unified_processor.rs
- **Line 146**: Add `use sinex_core::types::events::Event;`
- [✓ DONE] **Lines 37, 45, 120, 155**: Add trailing commas
- **Lines 259, 313**: Implement missing methods
- **Lines 126, 128, 131**: Define threshold constants
- **Lines 210-218, 268-276, 289-297**: Extract `fn build_scan_report()`

## SECTION 8: Automaton Services Refactoring

### analytics-automaton/lib.rs
- **Lines 46-57**: Update trait signature
- **Lines 68-84**: Add TODO or implement
- **Lines 105-148**: Extract to shared SDK

### analytics-automaton/unified_processor.rs
- **Lines 38-43**: Reconcile interfaces
- **Lines 92-98**: Add missing fields
- **Lines 113-122**: Fix field names

### content-automaton/lib.rs
- **Lines 46-57**: Extract common base
- **Lines 105-148**: Use configurable description

### content-automaton/unified_processor.rs
- **Lines 25-27**: Use `SinexError::validation()`
- **Lines 42-50**: Use ? operator

### pkm-automaton/lib.rs
- **Line 107**: Move import to module level
- **Lines 108-116**: Create shared factory

### search-automaton/lib.rs
- **Lines 105-148**: Extract to SDK

## SECTION 9: Document Ingestor & Remaining

### document-ingestor/lib.rs
- **Lines 70-73**: Use `SinexError::processing()`
- **Lines 87-90**: Add `debug!` log
- **Lines 213-218**: Extract helper or use `.with_context()`
- **Lines 278-300**: Consider builder
- **Lines 155-167**: Define material type constants

### rpc-dispatcher/lib.rs
- **Lines 17-30**: Define typed config struct
- **Lines 57-82**: Add TODO comments
- [✓ DONE] **Lines 64-70**: Simplify to single assignment
- **Lines 103-146**: Return NotImplemented errors
- **Lines 125-127**: Make time range configurable

### sensd/config.rs
- **Lines 124-146**: Add parse error context
- **Line 82**: Add NixOS comment

### sensd/main.rs
- **Lines 18-21**: Extract tracing helper
- **Lines 33-38**: Use graceful shutdown
- **Lines 41-51**: Simplify error context

### gateway/cascade_analyzer.rs
- **Lines 100-101**: Add temp table cleanup
- **Lines 167-190**: Extract UUID conversion helper

### gateway/replay_state_machine.rs
- **Lines 44-92**: Consider state transition table
- **Lines 138-169**: Use bon builder
- **Lines 124-135**: Use `#[derive(Default)]` [NOTE: Manual impl needed due to Utc::now() in updated_at field]

## PRIORITY EXECUTION ORDER

### Phase 1 - Immediate (High Impact, Low Effort):
1. Delete all obsolete files
2. Add all `#[must_use]` and `#[inline]` attributes
3. Extract all magic numbers to constants
4. Fix all `unwrap()` calls with `expect()` messages

### Phase 2 - Quick Refactors (Medium Impact, Small Effort):
5. Create all suggested helper functions
6. Apply all derive macros where manual impls exist
7. Fix all UTF-8 unsafe operations
8. Use established error patterns everywhere

### Phase 3 - Systematic Improvements (High Impact, Moderate Effort):
9. Replace custom errors with SinexError throughout
10. Extract all shared implementations in automata
11. Use SeaQuery instead of manual SQL
12. Complete all missing implementations or add proper TODOs with issue references

This plan covers ALL 394 Rust files analyzed with specific line-by-line improvements identified by the 10 parallel refactoring agents.