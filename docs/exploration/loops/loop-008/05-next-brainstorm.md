# Loop 008 - Next Analysis Brainstorm

- Event schema coverage vs emitted events (find emitted event types without registered schemas).
- Confirmation buffer backpressure and drop policy under load.
- Telemetry event volume vs rate limiting configuration.
- DLQ requeue semantics (idempotency and retry storms).
