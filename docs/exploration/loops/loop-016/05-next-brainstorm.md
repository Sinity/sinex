# Loop 016 - Next Analysis Ideas

- Run schema generation and compare new registry entries against current `schemas/v1`.
- Check feature flags on telemetry payloads to see if inventory registration is conditional.
- Build a diff of inventory payload count vs registry entry count.
- Inspect schema sync logic to ensure telemetry payloads are eligible for DB insertion.
