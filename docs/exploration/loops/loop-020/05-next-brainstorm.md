# Loop 020 - Next Analysis Ideas

- Check migrations to confirm `gitops_schema_sources` is created and whether any seed data exists.
- Search for schema sync tooling in scripts or external repos (if available).
- Add a diff between schema registry and inventory after parsing `define_event_payload!` macros.
- Investigate whether registry-only `system.*_historical` schemas are documented or deprecated.
