# Loop 018 - Next Analysis Ideas

- Inspect `sinex_schemas.gitops_schema_sources` for external schema sources.
- Search for `Event::dynamic` or `test_event` usage for registry-only sources.
- Determine which components emit `shell.*` or `terminal.kitty` events.
- Evaluate whether schema-only sources should be pruned or re-typed.
