# Loop 021 - Next Analysis Ideas

- Identify any deployment scripts or ops docs that mention populating `gitops_schema_sources`.
- Compare schema registry to inventory after including macro-defined payloads.
- Audit hard-coded source/event_type strings in `EventBuilder` for schema coverage.
- Investigate `journald/satellite.heartbeat` schema origin and intended emitter.
