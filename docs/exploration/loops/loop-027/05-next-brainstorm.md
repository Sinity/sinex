Next analysis brainstorm
- Retry vs fail-fast map for NATS/DB operations and consistent policy usage.
- Health check depth audit for gateway, ingestd, and node services.
- Schema validation coverage: which event types have schemas and which bypass validation.
