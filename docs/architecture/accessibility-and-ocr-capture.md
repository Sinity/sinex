# Accessibility And OCR Capture

**Status:** dissolved into issue tracking. The substantive contracts
that lived here — AT-SPI2 event surfaces and whitelist of app classes,
`AccessibilityContextPayload` shape, screen-region OCR trigger modes,
two-source-material-rows-per-capture pattern, LLM visual description
parking spot, privacy invariants, and the boundaries list — now live in
[issue #1043 (feat(capture): add audio transcripts and screen OCR
streams)](https://github.com/Sinity/sinex/issues/1043) as a design
comment.

Originating design issues `#391` (screen-region OCR) and `#396`
(AT-SPI2) are closed. `#1043` is the live tracking issue.

Privacy admission policy belongs to `#1042`. Privacy private-mode
suppression belongs to `#1071`. Operator audit/redact/export workflows
belong to `#1072`.

**Related:** `docs/architecture/desktop-capture-substrate.md`,
`docs/architecture/document-layer-v1.md`,
`docs/architecture/embedding-runtime.md`.
