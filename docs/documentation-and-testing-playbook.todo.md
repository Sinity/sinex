- [x] Add `doc/README.md` and module deep dives for `crate/lib/sinex-services`, replace `docs/` with `doc/`, and wire `#![doc = include_str!(...)]` into `src/lib.rs` plus any major modules.
- [x] Create `doc/` tree for `crate/core/sinex-ingestd` and include crate + module docs via `#![doc = include_str!(...)]`, referencing global ingestion architecture materials where helpful.
- [x] Introduce `doc/` structure for `crate/core/sinex-rpc-dispatcher`, add crate overview, and surface it through rustdoc.
- [x] Establish `doc/` documentation for `crate/core/sinex-sensd`, covering acquisition pipeline modules and linking from the crate root.
- [x] Add crate-level documentation (`doc/README.md` + module deep dives) for `crate/satellites/sinex-analytics-automaton` and expose via rustdoc includes.
- [x] Add crate-level documentation for `crate/satellites/sinex-system-satellite` with module pages and rustdoc wiring.
- [x] Audit remaining satellites/automata for missing `doc/` directories (content, fs-watcher, health-aggregator, pkm, search, terminal-command-canonicalizer, document-ingestor) and add documentation plus includes where absent.
- [x] Ensure all newly added crate docs cross-link to relevant materials under `docs/` (architecture, roadmap, etc.) when broader background exists.
- [x] Relocate broad behavioural suites from `crate/lib/sinex-test-utils/src` into `crate/lib/sinex-test-utils/tests/`, leaving only helper-specific inline tests.
- [x] After documentation and test relocation, run `just check` and `just test` to validate the workspace, updating any references or fixtures that break.
