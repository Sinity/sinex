[workspace]
members = [
  # Library crates  
  "crate/lib/sinex-core",
  "crate/lib/sinex-schema",
  "crate/lib/sinex-macros",
  "crate/lib/sinex-satellite-sdk",
  "crate/lib/sinex-processor-runtime",
  "crate/lib/sinex-services",
  # Re-enabled to ensure tests compile
  "crate/lib/sinex-test-utils",
  
  # Core runtime crates
  "crate/core/sinex-ingestd",
  "crate/core/sinex-gateway",
  "crate/core/sinex-rpc-dispatcher",
  
  # Satellite crates
  "crate/satellites/sinex-terminal-satellite",
  "crate/satellites/sinex-desktop-satellite",
  "crate/satellites/sinex-system-satellite",
  "crate/satellites/sinex-fs-watcher",
  "crate/satellites/sinex-terminal-command-canonicalizer",
  "crate/satellites/sinex-health-aggregator",
  "crate/satellites/sinex-document-ingestor",
  "tests/e2e",

  # Automaton crates
  "crate/satellites/sinex-analytics-automaton",
  "crate/satellites/sinex-search-automaton",
  "crate/satellites/sinex-content-automaton",
  "crate/satellites/sinex-pkm-automaton",

  # Keep experimental automata disabled until stabilized
  # "crate/satellites/sinex-content-automaton",
  # "crate/satellites/sinex-pkm-automaton",
  
  # Test modules are integration tests within the main workspace
  # "test/*", # Removed - tests are integration tests, not separate workspace members
  
  # Disabled/folded crates
  # "crate/sinex-annex", # Folded into sinex-satellite-sdk
  # "crate/sinex-preflight", # Folded into sinex-satellite-sdk
  # "crate/sinex-nats", # Folded into sinex-satellite-sdk
  # "crate/sinex-telemetry", # Folded into sinex-core
  # "crate/sinex-schema-manager", # Folded into sinex-core
]
resolver = "2"

# Clippy configuration for zero warnings enforcement
[workspace.lints.rust]
unreachable_pub = { level = "forbid", priority = -1 }
unused_crate_dependencies = { level = "forbid", priority = -1 }

[workspace.lints.clippy]
# Treat all clippy warnings as errors for production code
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
# Allow some pedantic lints that are too noisy
module_name_repetitions = "allow"
similar_names = "allow"
too_many_lines = "allow"
# Specific lints we want to enforce strictly
too_many_arguments = "deny"
type_complexity = "deny"
cognitive_complexity = "warn"


[workspace.package]
version = "0.4.2"
edition = "2021"
authors = ["Sinex Exocortex Contributors"]
repository = "https://github.com/sinex/exocortex"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.47", features = ["full"] }
async-trait = "0.1.88"

# Database
sqlx = { version = "0.8", default-features = false, features = [
  "runtime-tokio-rustls",
  "postgres",
  "uuid",
  "chrono",
  "json",
  "macros",
  "migrate",
  "bigdecimal",
] }
sea-query = { version = "0.31", features = ["with-chrono", "with-uuid", "with-json", "postgres-array"] }

# Serialization
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.141"

# Error handling
anyhow = "1.0.98"
thiserror = "2.0.12"
color-eyre = "0.6.5"
serde_path_to_error = "0.1.17"

# Path handling
camino = { version = "1.1", features = ["serde1"] }

# Logging
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.19", features = ["env-filter", "json"] }

# IDs and time
ulid = { version = "1.2.1", features = ["serde"] }
uuid = { version = "1.17.0", features = ["v4", "serde"] }
chrono = { version = "0.4.41", features = ["serde"] }

# HTTP and metrics
axum = "0.7"

# Utils
rand = "0.8"
once_cell = "1.21.3"
blake3 = "1.8.2"
glob = "0.3"
toml = "0.8"
fastrand = "2.0"
ahash = "0.8"
mimalloc = { version = "0.1", default-features = false }

# Builder pattern
bon = "2.3"

# Data structures
indexmap = "2.0"

# Retry logic
tokio-retry = "0.3"

# Message bus
async-nats = "0.33"

# System integration
sd-notify = "0.4"

# Additional utilities
tokio-stream = "0.1"
futures = "0.3"
dirs = "5.0"
lazy_static = "1.5"
base64 = "0.22"

# File system watching
notify = "6.1"

# Testing
proptest = "1"
rstest = "0.18"
insta = { version = "1.34", features = ["json", "yaml", "redactions"] }
tracing-test = "0.2"
similar-asserts = "1.5"

# System
gethostname = "0.4"
clap = { version = "4.5.42", features = ["derive", "env"] }
which = "5.0"

# Configuration and validation
regex = "1.11.1"
url = "2"
unicode-normalization = "0.1"
schemars = { version = "0.8", features = ["chrono", "uuid1"] }
tempfile = "3.0"
jsonschema = "0.17"
validator = { version = "0.18", features = ["derive"] }
figment = { version = "0.10.19", features = ["toml", "yaml", "json", "env"] }
humantime-serde = "1.1"

# Build-time registry
inventory = "0.3"

# Cryptography
sha2 = "0.10"
hex = "0.4"

# Database (for Atuin integration)
rusqlite = { version = "0.32.1", features = ["bundled"] }

# Workspace-level integration tests package
[package]
name = "sinex-workspace"
version = "0.4.2"
edition = "2021"
authors = ["Sinex Exocortex Contributors"]
repository = "https://github.com/sinex/exocortex"
license = "MIT OR Apache-2.0"

[dev-dependencies]
lazy_static = { workspace = true }
regex = { workspace = true }
jsonschema = { workspace = true }
which = { workspace = true }
rstest = { workspace = true }
proptest = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
tracing-test = { workspace = true }
uuid = { workspace = true }
color-eyre = { workspace = true }
sinex-core = { path = "crate/lib/sinex-core", features = ["testing"] }
sinex-test-utils = { path = "crate/lib/sinex-test-utils" }
blake3 = { workspace = true }
once_cell = { workspace = true }
async-nats = { workspace = true }

[dependencies]
# Test utilities and dependencies for workspace-level integration tests
sinex-satellite-sdk = { path = "crate/lib/sinex-satellite-sdk" }

# (Removed duplicate optional test deps; consolidated under dev-dependencies above)

# Speed-optimized profiles for development
[profile.dev]
# Fast compilation for development
opt-level = 0
debug = true
split-debuginfo = "unpacked" # Faster debug info handling
incremental = true
codegen-units = 256          # Maximum parallelism for fast compilation
