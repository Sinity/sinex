//! Source modules — combine mechanisms + parsers per source.
//!
//! Each submodule defines one source unit by binding the SDK adapter to a
//! source-specific parser. Currently empty as the source-worker ships only
//! the `NoopSourceUnit` smoke source defined in `crate::noop`.
