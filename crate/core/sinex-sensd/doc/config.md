# Configuration

`sensd` configuration covers sensor adapters, buffer sizing, and retention
policies. The `config` module provides typed structures and validation helpers
so binaries and tests can compose consistent setups.

Document new configuration fields here whenever additional sensors or features
are introduced.
