//! Document source contracts.
//!
//! `staging.rs` carries the descriptor, binding, and parser dispatch.
//! `runtime.rs` carries the imperative `DocumentSourceDriver` runtime.

pub mod runtime;
pub mod staging;
