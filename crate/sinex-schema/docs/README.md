# sinex-schema Documentation

This directory owns schema-local contracts that need more context than Rust
comments but should not live as global architecture records.

- `document_layer.md` — document parser projection tables and current v1 scope
