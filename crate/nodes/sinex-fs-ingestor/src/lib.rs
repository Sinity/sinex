//! Stub crate — the fs source unit has been folded into
//! `sinex-source-worker::sources::fs` (Wave B). The orchestrator will delete
//! this crate in a synchronized commit; this stub keeps the workspace
//! buildable until then by emitting no symbols and no `inventory::submit!`
//! calls.
