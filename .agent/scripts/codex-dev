#!/usr/bin/env bash
set -euo pipefail

# Run Codex CLI inside the repo's direnv/devShell environment.
# Useful for VSCode's Codex extension: point the CLI path at this script.

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo_root"

exec direnv exec . codex "$@"
