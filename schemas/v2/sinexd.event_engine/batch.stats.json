{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Ingestd batch processing statistics\n\nEmitted after each batch is processed by the `JetStream` consumer.\nCaptures throughput, latency, and schema validation coverage data for batch processing.",
  "properties": {
    "batch_size": {
      "description": "Number of events in this batch",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "confirmation_durability_gaps": {
      "description": "Cumulative count of confirmation-durability gaps: events were persisted\nbut the confirmation publish to the Confirmations stream failed\nnon-transiently. Non-zero is a durable signal of pipeline split-brain.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "events_deferred": {
      "description": "Events deferred to retry",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "events_failed": {
      "description": "Events that failed processing",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "fetch_to_ack_ms": {
      "description": "Time from fetch to ack in milliseconds",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "had_derived": {
      "description": "Whether this batch contained derived events",
      "type": "boolean"
    },
    "insert_path": {
      "description": "Insert path used: \"copy\" or \"`query_builder`\"",
      "type": "string"
    },
    "suspicious_future_ts_orig": {
      "description": "Cumulative count of events whose `ts_orig` is implausibly far in the future.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "telemetry_publish_failures": {
      "description": "Cumulative count of failures emitting observer telemetry gauges.\nNon-zero indicates the self-observation NATS channel is degraded.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "validation_coverage_pct": {
      "description": "Schema coverage percentage: events with a schema / total validated (excluding skipped)",
      "format": "double",
      "type": "number"
    },
    "validation_invalid": {
      "description": "Cumulative count of events that failed schema validation",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "validation_no_schema": {
      "description": "Cumulative count of events with no registered schema",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "validation_schema_not_found": {
      "description": "Cumulative count of events whose schema was not found in the registry",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "validation_skipped": {
      "description": "Cumulative count of events where validation was skipped (disabled)",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "validation_valid": {
      "description": "Cumulative count of events that passed schema validation",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "batch_size",
    "fetch_to_ack_ms",
    "events_deferred",
    "events_failed",
    "had_derived",
    "insert_path",
    "validation_valid",
    "validation_skipped",
    "validation_no_schema",
    "validation_schema_not_found",
    "validation_invalid",
    "validation_coverage_pct",
    "suspicious_future_ts_orig",
    "telemetry_publish_failures",
    "confirmation_durability_gaps"
  ],
  "title": "EventEngineBatchStatsPayload",
  "type": "object",
  "x-sinex-event-type": "batch.stats",
  "x-sinex-source": "sinexd.event_engine",
  "x-sinex-version": "2.2.0"
}
