{
  "$defs": {
    "DateTime": {
      "description": "RFC 3339 formatted date-time string",
      "format": "date-time",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "One commit observation parsed from a git repository's history (#1053).\n\nEach commit is one event. The `commit_sha` is the full 40-hex SHA-1\nof the commit object. Combined with `repo_path` it forms a globally-unique\noccurrence key (forks/mirrors share SHAs, so the repo path is necessary).\n\nStats fields (`files_changed_count`, `insertions`, `deletions`) are omitted\nfor merge commits when git cannot attribute them cleanly.",
  "properties": {
    "author_email": {
      "description": "Author e-mail (from `git log --format=%aE`).",
      "type": "string"
    },
    "author_name": {
      "description": "Author display name (from `git log --format=%aN`).",
      "type": "string"
    },
    "author_timestamp": {
      "$ref": "#/$defs/DateTime",
      "description": "Author timestamp (seconds since Unix epoch) as recorded in the commit\nobject. Used as `ts_orig`."
    },
    "body": {
      "description": "Everything after the subject line (may be empty).",
      "type": [
        "string",
        "null"
      ]
    },
    "commit_sha": {
      "description": "Full 40-character hex SHA-1 of the commit.",
      "type": "string"
    },
    "committer_email": {
      "description": "Committer e-mail (from `git log --format=%cE`).",
      "type": "string"
    },
    "committer_name": {
      "description": "Committer display name (from `git log --format=%cN`).",
      "type": "string"
    },
    "deletions": {
      "description": "Lines deleted. None when stats are unavailable.",
      "format": "uint32",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "files_changed_count": {
      "description": "Number of files changed. None for merge commits and when\n`--shortstat` is unavailable.",
      "format": "uint32",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "insertions": {
      "description": "Lines added. None when stats are unavailable.",
      "format": "uint32",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "parent_count": {
      "description": "Number of parent commits. 0 = root commit, 1 = ordinary commit,\n2+ = merge commit.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "repo_path": {
      "description": "Absolute path to the repository root on disk.",
      "type": "string"
    },
    "subject": {
      "description": "First line of the commit message.",
      "type": "string"
    }
  },
  "required": [
    "commit_sha",
    "repo_path",
    "author_name",
    "author_email",
    "committer_name",
    "committer_email",
    "subject",
    "parent_count",
    "author_timestamp"
  ],
  "title": "GitCommitPayload",
  "type": "object",
  "x-sinex-event-type": "commit.created",
  "x-sinex-source": "git",
  "x-sinex-version": "1.0.0"
}
