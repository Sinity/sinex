export NIXPKGS_ALLOW_UNFREE=1
use flake

# Check if development stack is running (non-blocking)
if command -v pg_isready &> /dev/null && ! pg_isready -q 2>/dev/null; then
    echo "💡 Development stack not running. Start with: cargo xtask stack start"
fi
